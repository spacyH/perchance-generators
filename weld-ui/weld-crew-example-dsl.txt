$meta
  title = weld.crew examples
  description = Live demo of weld.crew -- a tool-using AI agent that runs inside the generator on your own model, with durable memory, a thought scratchpad, and named workspaces over weld.skybridge. Watch the ReAct loop think, call tools, read results, and finish; memory and scratchpad persist across runs. Uses your own model when Weld Companion has one configured, otherwise a scripted demo reasoner so the loop is still visible. Imports weld.skybridge and weld.crew.
  tags = weld, crew, agent, ai, react, tools, memory, example, plugin

weldSkybridge = {import:weld-skybridge-plugin}
weldCrew     = {import:weld-crew-plugin}
