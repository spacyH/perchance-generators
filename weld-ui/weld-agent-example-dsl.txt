$meta
  title = weld.agent examples
  description = Live demo of weld.agent -- a tool-using AI agent that runs inside the generator on your own model, with durable memory, a thought scratchpad, and named workspaces over weld.skybridge. Watch the ReAct loop think, call tools, read results, and finish; memory and scratchpad persist across runs. Uses your own model when Weld Companion has one configured, otherwise a scripted demo reasoner so the loop is still visible. Imports weld.skybridge and weld.agent.
  tags = weld, agent, ai, react, tools, memory, example, plugin

  weldSkybridge = {import:weld-skybridge-plugin}
  weldAgent     = {import:weld-agent-plugin}
