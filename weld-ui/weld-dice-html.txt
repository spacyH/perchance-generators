<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.dice<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-dice-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.dice</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">Randomness for generators: dice notation, weighted tables, draw-without-replacement decks, shuffles, and a seeded RNG for reproducible runs. Pure and dependency-free.</p>
</div>
<h2>Install</h2><pre data-codeid="install"></pre>
<h2>Dice notation</h2><div class="sig" data-codeid="sig_roll"></div>
<p>Supports <code>NdS</code>, flat <code>+N/-N</code>, keep/drop (<code>kh/kl/dh/dl</code>), exploding (<code>!</code>), and <code>adv</code>/<code>dis</code>.</p>
<pre data-codeid="roll_ex"></pre>
<h2>Tables &amp; picks</h2>
<div class="sig" data-codeid="sig_weighted"></div><div class="sig" data-codeid="sig_pick"></div>
<div class="sig" data-codeid="sig_deck"></div>
<pre data-codeid="table_ex"></pre>
<h2>Seeded RNG</h2><div class="sig" data-codeid="sig_rng"></div>
<pre data-codeid="rng_ex"></pre>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-dice = \{import:weld-dice-plugin\}',
  'sig_roll': 'weld.dice.roll(notation, opts) -> \{ total, rolls, kept, notation \}',
  'roll_ex': 'weld.dice.roll(\'4d6kh3\');     // ability score: keep highest 3\nweld.dice.roll(\'2d20kh1\');    // advantage\nweld.dice.rollTotal(\'1d20+5\'); // 18',
  'sig_weighted': 'weld.dice.weighted(entries, opts) -> value',
  'sig_pick': 'weld.dice.pick(arr) / shuffle(arr) / sample(arr, k)',
  'sig_deck': 'weld.dice.deck(items, opts) -> \{ draw, drawN, remaining, reset, size \}',
  'table_ex': 'weld.dice.weighted(\{ common: 70, rare: 25, legendary: 5 \});\nvar deck = weld.dice.deck(cards);\ndeck.draw();  // no repeats until empty',
  'sig_rng': 'weld.dice.rng(seed) -> \{ next, int, pick, bool \}',
  'rng_ex': 'var rng = weld.dice.rng(\'seed-123\');\nrng.int(1, 6);  // reproducible across runs'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.dice) return window.weld.dice;

// -----------------------------------------------------------------
// weld.dice: the randomness primitives Perchance generators reach for,
// in one tested place. Dice-notation roller with keep/drop and
// advantage, weighted picks, decks that draw without replacement, and
// a seedable RNG so a generator can be made reproducible. No model, no
// DOM, no dependencies.
// -----------------------------------------------------------------

// mulberry32: tiny, fast, seedable PRNG. Returns a function -> [0,1).
function mulberry32(seed) {
  var a = seed >>> 0;
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    var t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function hashSeed(input) {
  var str = String(input);
  var h = 2166136261 >>> 0;
  for (var i = 0; i < str.length; i++) {
    h ^= str.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return h >>> 0;
}

// The default stream uses Math.random; rng(seed) returns a seeded one.
var defaultRand = Math.random;

function makeRng(seed) {
  var s = (typeof seed === 'number') ? seed : hashSeed(seed);
  var fn = mulberry32(s);
  return {
    next: fn,
    int: function (min, max) { return Math.floor(fn() * (max - min + 1)) + min; },
    pick: function (arr) { return arr[Math.floor(fn() * arr.length)]; },
    bool: function (p) { return fn() < (p == null ? 0.5 : p); }
  };
}

function randFor(opts) {
  if (opts && opts.rng && typeof opts.rng.next === 'function') return opts.rng.next;
  if (opts && typeof opts.rand === 'function') return opts.rand;
  if (opts && opts.seed != null) return makeRng(opts.seed).next;
  return defaultRand;
}

function rollDie(sides, rand) { return Math.floor(rand() * sides) + 1; }

// Parse and evaluate dice notation. Supports:
//   NdS            e.g. 3d6
//   dS             single die, e.g. d20
//   +N / -N        flat modifiers anywhere
//   khN / klN      keep highest / lowest N of the dice
//   dhN / dlN      drop highest / lowest N
//   !              exploding dice (reroll+add on max)
//   "adv" / "dis"  shorthand for 2d20kh1 / 2d20kl1
// Returns { total, rolls, kept, notation }.
function roll(notation, opts) {
  var rand = randFor(opts);
  var spec = String(notation == null ? '' : notation).trim().toLowerCase();
  if (spec === 'adv') spec = '2d20kh1';
  else if (spec === 'dis') spec = '2d20kl1';

  var total = 0;
  var allRolls = [];
  var keptAll = [];

  // Split into additive terms, keeping their signs.
  var terms = spec.replace(/\s+/g, '').match(/[+-]?[^+-]+/g) || [];
  for (var t = 0; t < terms.length; t++) {
    var term = terms[t];
    var sign = 1;
    if (term.charAt(0) === '+') term = term.slice(1);
    else if (term.charAt(0) === '-') { sign = -1; term = term.slice(1); }

    var dieMatch = term.match(/^(\d*)d(\d+)(!?)((?:k[hl]\d+|d[hl]\d+)?)$/);
    if (dieMatch) {
      var n = dieMatch[1] === '' ? 1 : parseInt(dieMatch[1], 10);
      var sides = parseInt(dieMatch[2], 10);
      var explode = dieMatch[3] === '!';
      var mod = dieMatch[4];
      var rolls = [];
      for (var i = 0; i < n; i++) {
        var r = rollDie(sides, rand);
        rolls.push(r);
        if (explode) {
          var guard = 0;
          while (r === sides && guard < 50) { r = rollDie(sides, rand); rolls.push(r); guard += 1; }
        }
      }
      allRolls = allRolls.concat(rolls);

      var kept = rolls.slice();
      if (mod) {
        var keep = mod.charAt(0) === 'k';
        var high = mod.charAt(1) === 'h';
        var count = parseInt(mod.slice(2), 10);
        var sorted = rolls.slice().sort(function (a, b) { return b - a; }); // desc
        if (keep) kept = high ? sorted.slice(0, count) : sorted.slice(sorted.length - count);
        else kept = high ? sorted.slice(count) : sorted.slice(0, sorted.length - count);
      }
      keptAll = keptAll.concat(kept);
      var sum = kept.reduce(function (a, b) { return a + b; }, 0);
      total += sign * sum;
    } else if (/^\d+$/.test(term)) {
      total += sign * parseInt(term, 10);
    }
    // Unknown terms are ignored rather than throwing.
  }

  return { total: total, rolls: allRolls, kept: keptAll, notation: String(notation) };
}

// Just the number.
function rollTotal(notation, opts) { return roll(notation, opts).total; }

// Uniform integer in [min, max] inclusive.
function between(min, max, opts) {
  var rand = randFor(opts);
  return Math.floor(rand() * (max - min + 1)) + min;
}

// Weighted pick. entries: array of { value, weight } OR a plain object
// { value: weight, ... }. Returns the chosen value.
function weighted(entries, opts) {
  var rand = randFor(opts);
  var list = [];
  if (Array.isArray(entries)) {
    list = entries.map(function (e) {
      if (e && typeof e === 'object' && 'weight' in e) return { value: e.value, weight: Math.max(0, +e.weight || 0) };
      return { value: e, weight: 1 };
    });
  } else if (entries && typeof entries === 'object') {
    list = Object.keys(entries).map(function (k) { return { value: k, weight: Math.max(0, +entries[k] || 0) }; });
  }
  var total = list.reduce(function (a, e) { return a + e.weight; }, 0);
  if (total <= 0) return undefined;
  var r = rand() * total;
  for (var i = 0; i < list.length; i++) {
    r -= list[i].weight;
    if (r < 0) return list[i].value;
  }
  return list[list.length - 1].value;
}

function pick(arr, opts) {
  if (!arr || !arr.length) return undefined;
  var rand = randFor(opts);
  return arr[Math.floor(rand() * arr.length)];
}

// Fisher-Yates shuffle (returns a new array).
function shuffle(arr, opts) {
  var rand = randFor(opts);
  var a = arr.slice();
  for (var i = a.length - 1; i > 0; i--) {
    var j = Math.floor(rand() * (i + 1));
    var tmp = a[i]; a[i] = a[j]; a[j] = tmp;
  }
  return a;
}

// Sample k distinct items (draw without replacement, single shot).
function sample(arr, k, opts) {
  return shuffle(arr, opts).slice(0, k);
}

// A deck: draws without replacement until empty, then optionally reshuffles.
// deck(items, { seed, reshuffle }) -> { draw, drawN, remaining, reset, size }
function deck(items, opts) {
  opts = opts || {};
  var rand = randFor(opts);
  var source = (items || []).slice();
  var pile = shuffle(source, { rand: rand });

  function draw() {
    if (!pile.length) {
      if (opts.reshuffle) pile = shuffle(source, { rand: rand });
      else return undefined;
    }
    return pile.shift();
  }
  function drawN(n) {
    var out = [];
    for (var i = 0; i < n; i++) {
      var c = draw();
      if (c === undefined) break;
      out.push(c);
    }
    return out;
  }
  return {
    draw: draw,
    drawN: drawN,
    remaining: function () { return pile.length; },
    reset: function () { pile = shuffle(source, { rand: rand }); },
    size: function () { return source.length; }
  };
}

window.weld.dice = {
  roll: roll,
  rollTotal: rollTotal,
  between: between,
  weighted: weighted,
  pick: pick,
  shuffle: shuffle,
  sample: sample,
  deck: deck,
  rng: makeRng,
  version: '1.0.0'
};

return window.weld.dice;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
