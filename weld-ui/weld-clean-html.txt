<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.clean<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-clean-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.clean</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">Scrub raw model text before you store or render it: trim a dangling sentence after truncation, strip role-name bleed, collapse repetition, balance code fences, drop hidden blocks, and apply your own regex rules.</p>
</div>
<h2>Install</h2><pre data-codeid="install"></pre>
<h2>Quick start</h2><p>Pass a weld.stream result straight in; clean() trims the partial sentence only when the output was truncated.</p><pre data-codeid="quick"></pre>
<h2>The pipeline</h2><div class="sig" data-codeid="sig_clean"></div>
<table><tr><th>opt</th><th>default</th><th>does</th></tr>
<tr><td><code>roleNames</code></td><td>&mdash;</td><td>names to strip as "Name:" bleed; \[\] enables generic mode</td></tr>
<tr><td><code>collapse</code></td><td>true</td><td>collapse repeated lines / character runs</td></tr>
<tr><td><code>fences</code></td><td>true</td><td>close an unbalanced code fence</td></tr>
<tr><td><code>hidden</code></td><td>true</td><td>remove hidden-from-AI marker blocks</td></tr>
<tr><td><code>rules</code></td><td>&mdash;</td><td>ordered find/replace rules</td></tr></table>
<h2>Individual fixers</h2>
<div class="sig" data-codeid="sig_trim"></div><div class="sig" data-codeid="sig_role"></div>
<div class="sig" data-codeid="sig_collapse"></div><div class="sig" data-codeid="sig_fences"></div>
<div class="sig" data-codeid="sig_hidden"></div><div class="sig" data-codeid="sig_rules"></div>
<pre data-codeid="rules_ex"></pre>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-clean = \{import:weld-clean-plugin\}',
  'quick': 'var r = await weld.stream.run(\{ instruction: prompt \});\nvar text = weld.clean.clean(r, \{ roleNames: \[\'Bot\'] \});  // trims if r.truncated',
  'sig_clean': 'weld.clean.clean(input, opts) -> string',
  'sig_trim': 'weld.clean.trimPartial(text) -> string',
  'sig_role': 'weld.clean.stripRolePrefix(text, names?) -> string',
  'sig_collapse': 'weld.clean.collapseRepeats(text, opts?) -> string',
  'sig_fences': 'weld.clean.balanceFences(text) -> string',
  'sig_hidden': 'weld.clean.stripHidden(text) -> string',
  'sig_rules': 'weld.clean.rules(text, ruleList) -> string',
  'rules_ex': 'weld.clean.rules(text, \[\n  \{ find: \'teh\', replace: \'the\', flags: \'gi\' \},\n  \{ find: /\\s+$/, replace: \'\' \}\n]);'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.clean) return window.weld.clean;

// -----------------------------------------------------------------
// weld.clean: the "scrub the raw model text" step every chat app
// re-implements. A text model returns prose that often needs fixing
// before you store or render it:
//   - a half-finished trailing sentence when the output was truncated
//   - the model echoing "Name:" or hallucinating the next speaker turn
//   - repetition loops (the same line/word over and over)
//   - an unclosed ``` code fence that breaks the markdown renderer
//   - leftover hidden-from-AI marker blocks
// Each fixer is exposed on its own; clean() runs a configurable pipe.
// Everything here is pure string -> string, no dependencies.
// -----------------------------------------------------------------

var HIDDEN_START = '<!--hidden-from-ai-start-->';
var HIDDEN_END   = '<!--hidden-from-ai-end-->';
var FENCE = String.fromCharCode(96, 96, 96); // three backticks

// Remove an incomplete final sentence. Use when a result was truncated
// (stopReason "artificial") so you don't show a chopped-off fragment.
function trimPartial(text) {
  var s = String(text == null ? '' : text);
  if (!s) return s;
  var trimmed = s.replace(/\s+$/, '');
  // Already ends cleanly on terminal punctuation or a closing quote/bracket.
  if (/[.!?\u2026\u3002\uff01\uff1f]["'\u201d\u2019)\]]?$/.test(trimmed)) return trimmed;
  // Otherwise cut back to the last sentence terminator we can find.
  var m = trimmed.match(/[\s\S]*[.!?\u2026\u3002\uff01\uff1f]["'\u201d\u2019)\]]?/);
  if (m && m[0].trim().length > 0) return m[0].replace(/\s+$/, '');
  return trimmed; // no terminator at all: leave it as-is rather than blank it
}

// Strip a leading "Name:" the model echoed, and cut the reply at the
// point where it starts speaking AS another turn (role bleed).
// names: optional array of speaker names to police; if omitted, any
// capitalized "Word:" or "Word :" at the start of a line is treated
// as a turn boundary.
function stripRolePrefix(text, names) {
  var s = String(text == null ? '' : text);
  if (!s) return s;

  function speakerAlt() {
    if (names && names.length) {
      var esc = names.map(function (n) {
        return String(n).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
      });
      return '(?:' + esc.join('|') + ')';
    }
    return '[A-Z][\\w .\u2019\'-]{0,30}';
  }
  var alt = speakerAlt();

  // 1. Drop a single leading "Name:" prefix on the whole reply.
  s = s.replace(new RegExp('^\\s*' + alt + '\\s*:\\s*'), '');

  // 2. Cut at the first mid-reply line that starts a new speaker turn.
  var boundary = new RegExp('\\n\\s*' + alt + '\\s*:\\s');
  var hit = s.search(boundary);
  if (hit !== -1) s = s.slice(0, hit);

  return s.replace(/\s+$/, '');
}

// Collapse runaway repetition: identical adjacent lines, identical
// adjacent sentences, and long single-character runs.
function collapseRepeats(text, opts) {
  opts = opts || {};
  var maxLineRepeat = opts.maxLineRepeat || 2;
  var maxCharRun = opts.maxCharRun || 8;
  var s = String(text == null ? '' : text);
  if (!s) return s;

  // Long single-character runs (e.g. "okayyyyyyyy", "!!!!!!!!").
  s = s.replace(/(.)\1{7,}/g, function (m, ch) {
    return new Array(maxCharRun + 1).join(ch);
  });

  // Identical adjacent lines beyond the allowed repeat count.
  var lines = s.split('\n');
  var out = [];
  var run = 0, prev = null;
  for (var i = 0; i < lines.length; i++) {
    var key = lines[i].trim();
    if (key !== '' && key === prev) {
      run += 1;
      if (run >= maxLineRepeat) continue;
    } else {
      run = 0; prev = key;
    }
    out.push(lines[i]);
  }
  return out.join('\n');
}

// Ensure triple-backtick code fences are balanced. If an odd fence is
// left open, append a closing one so the markdown renderer recovers.
function balanceFences(text) {
  var s = String(text == null ? '' : text);
  var count = 0, idx = s.indexOf(FENCE);
  while (idx !== -1) { count += 1; idx = s.indexOf(FENCE, idx + 3); }
  if (count % 2 === 1) {
    if (!/\n$/.test(s)) s += '\n';
    s += FENCE;
  }
  return s;
}

// Remove hidden-from-AI marker blocks and their contents.
function stripHidden(text) {
  var s = String(text == null ? '' : text);
  var start = s.indexOf(HIDDEN_START);
  while (start !== -1) {
    var end = s.indexOf(HIDDEN_END, start);
    if (end === -1) { s = s.slice(0, start); break; }
    s = s.slice(0, start) + s.slice(end + HIDDEN_END.length);
    start = s.indexOf(HIDDEN_START);
  }
  return s;
}

// Apply an ordered list of find/replace rules.
// rule = { find: string|RegExp, replace: string, flags?: string }
function applyRules(text, ruleList) {
  var s = String(text == null ? '' : text);
  if (!ruleList || !ruleList.length) return s;
  for (var i = 0; i < ruleList.length; i++) {
    var r = ruleList[i];
    if (!r || r.find == null) continue;
    var re;
    if (r.find instanceof RegExp) re = r.find;
    else re = new RegExp(String(r.find), r.flags == null ? 'g' : r.flags);
    s = s.replace(re, r.replace == null ? '' : String(r.replace));
  }
  return s;
}

// The pipeline. Each stage is opt-in; pass a result object straight
// from weld.stream and clean() will trimPartial only when truncated.
// opts:
//   truncated     (bool)    run trimPartial (auto-true if you pass result)
//   roleNames     (array)   names for stripRolePrefix; [] enables generic mode
//   roles         (bool)    enable stripRolePrefix even without names
//   collapse      (bool)    collapseRepeats (default true)
//   fences        (bool)    balanceFences (default true)
//   hidden        (bool)    stripHidden (default true)
//   rules         (array)   custom find/replace rules
//   trim          (bool)    trim outer whitespace (default true)
function clean(input, opts) {
  opts = opts || {};
  var text, truncated;
  if (input && typeof input === 'object' && 'text' in input) {
    // A weld.stream result object.
    text = input.text;
    truncated = (opts.truncated != null) ? opts.truncated : !!input.truncated;
  } else {
    text = input;
    truncated = !!opts.truncated;
  }
  var s = String(text == null ? '' : text);

  if (opts.hidden !== false) s = stripHidden(s);
  if (opts.roles || (opts.roleNames != null)) s = stripRolePrefix(s, opts.roleNames);
  if (opts.collapse !== false) s = collapseRepeats(s, opts);
  if (truncated) s = trimPartial(s);
  if (opts.rules) s = applyRules(s, opts.rules);
  if (opts.fences !== false) s = balanceFences(s);
  if (opts.trim !== false) s = s.replace(/^\s+|\s+$/g, '');
  return s;
}

window.weld.clean = {
  clean: clean,
  trimPartial: trimPartial,
  stripRolePrefix: stripRolePrefix,
  collapseRepeats: collapseRepeats,
  balanceFences: balanceFences,
  stripHidden: stripHidden,
  rules: applyRules,
  version: '1.0.0'
};

return window.weld.clean;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
