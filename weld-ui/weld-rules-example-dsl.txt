$meta
  title = weld.rules examples
  description = Interactive demo of weld.rules -- a declarative when-X-do-Y engine. Register metric getters and an action handler, declare rules as plain data, and tick the engine; hold-duration and cooldown control when actions fire.
  tags = weld, rules, automation, reactive, example, plugin

weldRules = {import:weld-rules-plugin}
