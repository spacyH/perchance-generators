$meta
  title       = weld.testrig examples
  description = Self-testing demo: runs assertions against itself and loaded weld plugins. Auto-runs on load. Export as text or JSON.
  tags        = weld, testrig, test, assert, example, plugin

weldUi      = {import:weld-ui-plugin}
weldTestrig = {import:weld-testrig-plugin}
weldQueue   = {import:weld-queue-plugin}
