$meta
  title = weld.session examples
  description = Interactive demo of weld.session -- a unified conversation timeline that owns a branching tree of text and image turns, with regenerate (sibling takes), swipe, branch, and serialize. Delegates to weld.stream / weld.image / weld.branching / weld.history.
  tags = weld, session, timeline, chat, example, plugin

  weldSession   = {import:weld-session-plugin}
  weldBranching = {import:weld-branching-plugin}
