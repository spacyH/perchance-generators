<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{--bg:#0d1117;--surface:#161b22;--ink:#e6edf3;--muted:#7d8590;--faint:#21262d;--accent:#4493f8;--green:#3fb950;--red:#f85149;--yellow:#d29922;--border:rgba(255,255,255,0.10);--font:'DM Sans',system-ui,sans-serif;--mono:'JetBrains Mono',monospace;}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.6 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}a:hover{text-decoration:underline;}
#page{max-width:880px;margin:0 auto;padding:36px 18px 96px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;font:12px var(--mono);color:var(--muted);padding:12px 0 16px;margin-bottom:22px;border-bottom:1px solid var(--border);}
.breadcrumb a{color:var(--muted);}.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}.breadcrumb .crumb-cur{color:var(--ink);}.breadcrumb .crumb-action a{color:var(--accent);}
.hdr{margin-bottom:24px;padding-bottom:18px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:12px;}
.hdr h1{font:700 26px var(--mono);letter-spacing:-0.5px;}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);padding:2px 8px;border-radius:4px;}
.hdr .lead{color:var(--muted);margin-top:8px;}
.drop{border:2px dashed var(--border);border-radius:12px;padding:46px 20px;text-align:center;cursor:pointer;transition:.15s;background:var(--surface);}
.drop:hover,.drop.over{border-color:var(--accent);background:rgba(68,147,248,0.06);}
.drop .big{font-size:17px;font-weight:600;margin-bottom:6px;}.drop .sub{color:var(--muted);font-size:13px;}
.hide{display:none!important;}
.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:18px;}
@media(max-width:640px){.grid{grid-template-columns:1fr;}}
.pane{background:var(--surface);border:1px solid var(--border);border-radius:10px;overflow:hidden;}
.pane .cap{font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;color:var(--muted);padding:10px 12px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;gap:8px;}
.cw{position:relative;display:flex;align-items:center;justify-content:center;padding:10px;min-height:200px;}
.checker{background-image:linear-gradient(45deg,#1c1c1c 25%,transparent 25%),linear-gradient(-45deg,#1c1c1c 25%,transparent 25%),linear-gradient(45deg,transparent 75%,#1c1c1c 75%),linear-gradient(-45deg,transparent 75%,#1c1c1c 75%);background-size:18px 18px;background-position:0 0,0 9px,9px -9px,-9px 0;background-color:#2b2b2b;}
canvas{max-width:100%;height:auto;display:block;border-radius:4px;}
#srcCanvas{cursor:crosshair;}
.controls{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:16px;margin-top:14px;}
.row{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:9px 0;}
.row .lbl{font:600 12px var(--mono);color:var(--muted);min-width:96px;}
.seg{display:inline-flex;border:1px solid var(--border);border-radius:6px;overflow:hidden;}
.seg button{background:transparent;color:var(--muted);border:0;padding:6px 11px;font:600 12px var(--font);cursor:pointer;}
.seg button.on{background:var(--accent);color:#fff;}
input[type=range]{flex:1;min-width:110px;accent-color:var(--accent);}
.val{font:12px var(--mono);color:var(--ink);min-width:42px;text-align:right;}
.btn{background:var(--accent);color:#fff;border:0;border-radius:6px;padding:9px 15px;font:600 13px var(--font);cursor:pointer;}
.btn.ghost{background:var(--faint);color:var(--ink);}
.btn:disabled{opacity:.4;cursor:not-allowed;}
.chip{display:inline-flex;align-items:center;gap:6px;background:var(--faint);border:1px solid var(--border);border-radius:20px;padding:3px 6px 3px 4px;font:11px var(--mono);}
.chip .sw{width:16px;height:16px;border-radius:50%;border:1px solid rgba(255,255,255,.2);}
.chip .x{cursor:pointer;color:var(--muted);font-weight:700;padding:0 3px;}.chip .x:hover{color:var(--red);}
.swrow{display:flex;flex-wrap:wrap;gap:6px;align-items:center;}
.hoverchip{display:inline-flex;align-items:center;gap:6px;font:11px var(--mono);color:var(--muted);}
.hoverchip .sw{width:14px;height:14px;border-radius:3px;border:1px solid rgba(255,255,255,.25);background:#000;}
.chk{display:inline-flex;align-items:center;gap:6px;font:13px var(--font);color:var(--ink);cursor:pointer;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);padding:10px 14px;border-radius:0 4px 4px 0;margin:16px 0;font-size:13px;color:#c9d1d9;}
input[type=text]{background:var(--bg);border:1px solid var(--border);border-radius:6px;color:var(--ink);padding:7px 10px;font:13px var(--font);flex:1;min-width:120px;}
.footer{margin-top:46px;padding-top:18px;border-top:1px solid var(--border);font:12px var(--mono);color:var(--muted);}.footer a{color:var(--muted);}
</style>
<div id="page">
  <div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>portrait studio<span class="crumb-sep">/</span><span class="crumb-cur">app</span></span><span class="crumb-action"><a href="https://perchance.org/weld-background-plugin">weld.background &rarr;</a></span></div>
  <div class="hdr">
    <div class="hdr-row"><h1>VN Portrait Studio</h1><span class="ver">built on weld</span></div>
    <p class="lead">Cut a character out of its background and drop the transparent portrait straight into a visual-novel scene. Pick the background colours with the eyedropper, tidy the edges, and export a PNG with alpha. Runs entirely in your browser.</p>
  </div>

  <div id="drop" class="drop">
    <div class="big">Tap to choose a character image</div>
    <div class="sub">or drag &amp; drop / paste &middot; your camera roll works on mobile</div>
    <input id="file" class="hide" type="file" accept="image/*">
  </div>

  <div id="work" class="hide">
    <div class="grid">
      <div class="pane">
        <div class="cap"><span>original &mdash; click to pick background</span><span id="hover" class="hoverchip"><span class="sw"></span><span id="hoverHex">--</span></span></div>
        <div class="cw"><canvas id="srcCanvas"></canvas></div>
      </div>
      <div class="pane">
        <div class="cap"><span>cut-out portrait (transparent)</span><span id="busy" class="hoverchip"></span></div>
        <div class="cw checker"><canvas id="outCanvas"></canvas></div>
      </div>
    </div>

    <div class="controls">
      <div class="row">
        <span class="lbl">eyedropper</span>
        <div class="swrow" id="swatches"></div>
        <button id="autoBtn" class="btn ghost">+ Auto-detect</button>
        <button id="clearBtn" class="btn ghost">Clear</button>
      </div>
      <div class="row">
        <span class="lbl">remove</span>
        <span class="seg" id="modeSeg">
          <button data-mode="global" class="on">Everywhere</button>
          <button data-mode="edges">Edges only</button>
        </span>
        <span class="hoverchip" id="modeHint">erases the picked colours wherever they appear</span>
      </div>
      <div class="row"><span class="lbl">tolerance</span><input id="tol" type="range" min="2" max="60" value="20"><span id="tolVal" class="val"></span></div>
      <div class="row"><span class="lbl">edge softness</span><input id="soft" type="range" min="0" max="30" value="8"><span id="softVal" class="val"></span></div>
      <div class="row">
        <span class="lbl">cleanup</span>
        <label class="chk"><input type="checkbox" id="tidy" checked> remove stray specks</label>
        <label class="chk"><input type="checkbox" id="isolate"> keep largest subject only</label>
        <label class="chk"><input type="checkbox" id="trimChk" checked> trim to subject</label>
      </div>
      <div class="row" style="margin-top:14px;">
        <button id="dlPortrait" class="btn" disabled>Download portrait PNG</button>
        <button id="reset" class="btn ghost">New image</button>
      </div>
    </div>

    <div class="pane" style="margin-top:18px;">
      <div class="cap"><span>in the scene &mdash; visual-novel preview</span><button id="dlScene" class="btn ghost" style="padding:4px 10px;font-size:11px;" disabled>Download scene</button></div>
      <div class="cw" style="background:#000;"><canvas id="vnCanvas" width="640" height="360"></canvas></div>
      <div style="padding:0 14px 16px;">
        <div class="row"><span class="lbl">scene</span>
          <span class="seg" id="sceneSeg">
            <button data-scene="dusk" class="on">Dusk</button>
            <button data-scene="sky">Sky</button>
            <button data-scene="room">Room</button>
          </span>
          <button id="sceneBtn" class="btn ghost">Upload scene</button>
          <input id="sceneFile" class="hide" type="file" accept="image/*">
        </div>
        <div class="row"><span class="lbl">position</span>
          <span class="seg" id="posSeg">
            <button data-pos="left">Left</button>
            <button data-pos="center" class="on">Center</button>
            <button data-pos="right">Right</button>
          </span>
          <span class="lbl" style="min-width:40px;">size</span><input id="scale" type="range" min="50" max="115" value="92"><span id="scaleVal" class="val"></span>
        </div>
        <div class="row"><span class="lbl">name</span><input id="nameInput" type="text" value="Aria" maxlength="24"></div>
        <div class="row"><span class="lbl">line</span><input id="lineInput" type="text" value="So this is where the story begins..." maxlength="160"></div>
      </div>
    </div>

    <div class="note">The eyedropper feeds <code>weld.background</code>'s multi-colour key &mdash; pick the sky, then the grass, then a shadow, and all three drop out at once. <strong>Everywhere</strong> removes those colours across the whole image (best for flat character art); <strong>Edges only</strong> flood-fills from the borders so a subject sharing the background colour is kept. Output is always transparent PNG, ready as a portrait.</div>
  </div>

  <footer class="footer"><span>&#169; 2026 therealwestninja.</span> &#183; <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183; <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a></footer>
</div>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.background) return window.weld.background;

// -----------------------------------------------------------------
// weld.background: take an image, drop its background, put a new one
// behind the subject. Built to run on a photo the user uploads from
// their phone or PC, entirely in the browser -- so the default engine
// is pure canvas (no model, no network) and works inside the Perchance
// sandbox where loading a big ML model is not guaranteed.
//
//   remove(src, opts)            -> canvas with the background erased
//   replace(src, background, o)  -> canvas with a new background behind
//   mask(src, opts)              -> the raw alpha mask
//   maskFromData(rgba, w, h, o)  -> pure mask math (testable, no canvas)
//   useSegmenter(fn)             -> swap in an AI cutout when you have one
//   fromFile / fromUrl / toDataUrl / toBlob / download  -> I/O helpers
//
// The default keying works best on a reasonably uniform background
// (a wall, sky, product backdrop, green/blue screen). For arbitrary
// scenes, register an AI segmenter via useSegmenter().
// -----------------------------------------------------------------

var MAX_DIM_DEFAULT = 1600; // cap working size so big phone photos stay fast
var segmenter = null;       // optional async (canvas) -> alpha mask

// ---- pure colour helpers (no DOM) ----
function dist(d, i, r, g, b) {
  var dr = (d[i] - r) / 255, dg = (d[i + 1] - g) / 255, db = (d[i + 2] - b) / 255;
  return Math.sqrt((dr * dr + dg * dg + db * db) / 3); // 0..1
}

// Estimate the background colour from the border pixels (trimmed mean).
function estimateBgColor(data, w, h) {
  var rs = [], gs = [], bs = [];
  function add(x, y) {
    var i = (y * w + x) * 4;
    rs.push(data[i]); gs.push(data[i + 1]); bs.push(data[i + 2]);
  }
  var step = Math.max(1, Math.floor(Math.max(w, h) / 200));
  for (var x = 0; x < w; x += step) { add(x, 0); add(x, h - 1); }
  for (var y = 0; y < h; y += step) { add(0, y); add(w - 1, y); }
  function trimmedMean(arr) {
    arr.sort(function (a, b) { return a - b; });
    var lo = Math.floor(arr.length * 0.1), hi = Math.ceil(arr.length * 0.9);
    var sum = 0, n = 0;
    for (var k = lo; k < hi; k++) { sum += arr[k]; n++; }
    return n ? Math.round(sum / n) : (arr.length ? arr[arr.length >> 1] : 255);
  }
  return [trimmedMean(rs), trimmedMean(gs), trimmedMean(bs)];
}

// Feathered alpha for a colour distance: 255 (keep) far from the key,
// 0 (drop) within tol, a smooth ramp across the softness band.
function alphaFor(d, tol, soft) {
  if (d <= tol) return 0;
  if (soft <= 0) return 255;
  if (d >= tol + soft) return 255;
  return Math.round(255 * (d - tol) / soft);
}

// Distance to the NEAREST of a list of key colours (for multi-pick).
function minDist(d, i, keys) {
  var m = 1;
  for (var k = 0; k < keys.length; k++) {
    var dd = dist(d, i, keys[k][0], keys[k][1], keys[k][2]);
    if (dd < m) m = dd;
  }
  return m;
}

// The core mask. Pure: operates on a raw RGBA array, returns a
// Uint8ClampedArray of per-pixel alpha (255 = foreground/keep).
// opts: { mode:'auto'|'chroma', keyColor:[r,g,b], keyColors (array of [r,g,b]),
//         tolerance, softness, contiguous (auto only), smooth }
// keyColors lets an eyedropper remove several background colours at once:
// a pixel is background if it is within tolerance of ANY key colour.
function maskFromData(data, w, h, opts) {
  opts = opts || {};
  var tol = opts.tolerance != null ? opts.tolerance : 0.18;
  var soft = opts.softness != null ? opts.softness : 0.10;
  var keys = (opts.keyColors && opts.keyColors.length)
    ? opts.keyColors
    : [opts.keyColor || estimateBgColor(data, w, h)];
  var n = w * h;
  var alpha = new Uint8ClampedArray(n);
  for (var p = 0; p < n; p++) alpha[p] = 255;

  var contiguous = (opts.mode === 'chroma') ? false
    : (opts.contiguous !== false); // auto defaults to contiguous

  if (!contiguous) {
    // Global colour key: erase any of the key colours everywhere.
    for (var q = 0; q < n; q++) {
      alpha[q] = alphaFor(minDist(data, q * 4, keys), tol, soft);
    }
  } else {
    // Flood fill from the borders: only erase background connected to
    // an edge, so a subject the same colour as the wall is kept.
    var visited = new Uint8Array(n);
    var stack = [];
    function consider(idx) {
      if (visited[idx]) return;
      visited[idx] = 1;
      var d = minDist(data, idx * 4, keys);
      if (d <= tol + soft) {
        alpha[idx] = alphaFor(d, tol, soft);
        stack.push(idx);
      }
      // else: a foreground edge -- leave alpha 255, do not spread through it
    }
    for (var bx = 0; bx < w; bx++) { consider(bx); consider((h - 1) * w + bx); }
    for (var by = 0; by < h; by++) { consider(by * w); consider(by * w + (w - 1)); }
    while (stack.length) {
      var idx2 = stack.pop();
      var px = idx2 % w, py = (idx2 - px) / w;
      if (px > 0) consider(idx2 - 1);
      if (px < w - 1) consider(idx2 + 1);
      if (py > 0) consider(idx2 - w);
      if (py < h - 1) consider(idx2 + w);
    }
  }

  if (opts.smooth !== false && soft > 0) smoothAlpha(alpha, w, h);
  return alpha;
}

// A light 3x3 average pass on the alpha to soften jagged edges.
function smoothAlpha(alpha, w, h) {
  var src = alpha.slice();
  for (var y = 1; y < h - 1; y++) {
    for (var x = 1; x < w - 1; x++) {
      var i = y * w + x;
      // only touch boundary pixels (where neighbours disagree)
      var a = src[i];
      if (a === 0 || a === 255) {
        var diff = false;
        if (src[i - 1] !== a || src[i + 1] !== a || src[i - w] !== a || src[i + w] !== a) diff = true;
        if (!diff) continue;
      }
      var sum = src[i] + src[i - 1] + src[i + 1] + src[i - w] + src[i + w]
        + src[i - w - 1] + src[i - w + 1] + src[i + w - 1] + src[i + w + 1];
      alpha[i] = Math.round(sum / 9);
    }
  }
}

// ---- canvas I/O ----
function makeCanvas(w, h) {
  var c = document.createElement('canvas');
  c.width = w; c.height = h;
  return c;
}

// Draw any source (canvas/image/ImageBitmap) onto a new canvas,
// optionally downscaled so the long side <= maxDim.
function toWorkingCanvas(srcImg, maxDim) {
  var sw = srcImg.naturalWidth || srcImg.width;
  var sh = srcImg.naturalHeight || srcImg.height;
  var scale = 1;
  if (maxDim && Math.max(sw, sh) > maxDim) scale = maxDim / Math.max(sw, sh);
  var w = Math.max(1, Math.round(sw * scale));
  var h = Math.max(1, Math.round(sh * scale));
  var c = makeCanvas(w, h);
  c.getContext('2d').drawImage(srcImg, 0, 0, w, h);
  return c;
}

function getData(canvas) {
  return canvas.getContext('2d').getImageData(0, 0, canvas.width, canvas.height);
}

// Resolve a source (canvas | image element | File/Blob | url string) to
// a working canvas (Promise).
function resolveSource(src, opts) {
  opts = opts || {};
  var maxDim = opts.maxDim != null ? opts.maxDim : MAX_DIM_DEFAULT;
  if (src && src.getContext) return Promise.resolve(toWorkingCanvas(src, maxDim));
  if (typeof Blob !== 'undefined' && src instanceof Blob) {
    return fromFile(src).then(function (c) { return toWorkingCanvas(c, maxDim); });
  }
  if (typeof src === 'string') {
    return fromUrl(src).then(function (c) { return toWorkingCanvas(c, maxDim); });
  }
  if (src && (src.naturalWidth || src.width)) return Promise.resolve(toWorkingCanvas(src, maxDim));
  return Promise.reject(new Error('weld.background: unsupported image source'));
}

function fromFile(file) {
  return new Promise(function (resolve, reject) {
    if (typeof FileReader === 'undefined') { reject(new Error('weld.background: FileReader unavailable')); return; }
    var fr = new FileReader();
    fr.onload = function () { resolve(fromUrl(fr.result)); };
    fr.onerror = function () { reject(fr.error); };
    fr.readAsDataURL(file);
  }).then(function (c) { return c; });
}

function fromUrl(url) {
  return new Promise(function (resolve, reject) {
    var img = new Image();
    // data: URLs are same-origin; remote URLs need CORS to read pixels.
    if (!/^data:/.test(String(url))) img.crossOrigin = 'anonymous';
    img.onload = function () {
      var c = makeCanvas(img.naturalWidth, img.naturalHeight);
      c.getContext('2d').drawImage(img, 0, 0);
      resolve(c);
    };
    img.onerror = function () { reject(new Error('weld.background: could not load image')); };
    img.src = url;
  });
}

// ---- public mask / remove ----
function mask(src, opts) {
  return resolveSource(src, opts).then(function (canvas) {
    if (segmenter) {
      return Promise.resolve(segmenter(canvas, opts)).then(function (m) {
        return { width: canvas.width, height: canvas.height, alpha: normalizeMask(m, canvas.width, canvas.height), canvas: canvas };
      });
    }
    var img = getData(canvas);
    var alpha = maskFromData(img.data, canvas.width, canvas.height, opts);
    return { width: canvas.width, height: canvas.height, alpha: alpha, canvas: canvas };
  });
}

// Accept several segmenter return shapes -> Uint8ClampedArray alpha.
function normalizeMask(m, w, h) {
  var n = w * h;
  if (m instanceof Uint8ClampedArray && m.length === n) return m;
  if (m && m.data && m.data.length === n * 4) { // ImageData: take alpha channel
    var a = new Uint8ClampedArray(n);
    for (var i = 0; i < n; i++) a[i] = m.data[i * 4 + 3];
    return a;
  }
  if (m && m.length === n) { // Float32 0..1 or Uint8 0..255
    var out = new Uint8ClampedArray(n), float = m[0] <= 1 && m[0] >= 0;
    // crude detection: assume 0..1 if max small; safer to scan
    var max = 0; for (var k = 0; k < n; k++) if (m[k] > max) max = m[k];
    var mul = max <= 1 ? 255 : 1;
    for (var j = 0; j < n; j++) out[j] = m[j] * mul;
    return out;
  }
  // Unknown: keep everything.
  var keep = new Uint8ClampedArray(n);
  for (var z = 0; z < n; z++) keep[z] = 255;
  return keep;
}

// remove(src, opts) -> canvas with the background made transparent.
function remove(src, opts) {
  return mask(src, opts).then(function (res) {
    var canvas = res.canvas;
    var ctx = canvas.getContext('2d');
    var img = ctx.getImageData(0, 0, canvas.width, canvas.height);
    var d = img.data, a = res.alpha;
    for (var p = 0; p < a.length; p++) {
      var cur = d[p * 4 + 3];
      d[p * 4 + 3] = Math.min(cur, a[p]);
    }
    if (opts && opts.despill) despillEdges(d, a, canvas.width, canvas.height, opts);
    var out = makeCanvas(canvas.width, canvas.height);
    out.getContext('2d').putImageData(img, 0, 0);
    return out;
  });
}

// Reduce key-colour fringe on partially transparent edge pixels.
function despillEdges(d, alpha, w, h, opts) {
  for (var p = 0; p < alpha.length; p++) {
    if (alpha[p] > 0 && alpha[p] < 255) {
      var i = p * 4;
      var avg = (d[i] + d[i + 1] + d[i + 2]) / 3;
      // pull each channel toward the local average to kill the spill tint
      d[i] = Math.round((d[i] + avg) / 2);
      d[i + 1] = Math.round((d[i + 1] + avg) / 2);
      d[i + 2] = Math.round((d[i + 2] + avg) / 2);
    }
  }
}

// ---- background painting + compositing ----
function paintBackground(ctx, w, h, background, original) {
  if (background == null || background === 'transparent') return Promise.resolve();
  if (typeof background === 'string') { ctx.fillStyle = background; ctx.fillRect(0, 0, w, h); return Promise.resolve(); }
  if (background && background.gradient) {
    var ang = (background.angle || 0) * Math.PI / 180;
    var x = Math.cos(ang), y = Math.sin(ang);
    var g = ctx.createLinearGradient(w / 2 - x * w / 2, h / 2 - y * h / 2, w / 2 + x * w / 2, h / 2 + y * h / 2);
    var stops = background.gradient;
    for (var s = 0; s < stops.length; s++) {
      var pos = stops.length === 1 ? 0 : s / (stops.length - 1);
      g.addColorStop(pos, stops[s]);
    }
    ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); return Promise.resolve();
  }
  if (background && background.blur != null && original) {
    var prev = ctx.filter;
    ctx.filter = 'blur(' + background.blur + 'px)';
    drawCover(ctx, original, w, h);
    ctx.filter = prev || 'none';
    return Promise.resolve();
  }
  // Otherwise treat as an image source.
  var fit = (background && background.fit) || 'cover';
  var imgSrc = (background && background.image) ? background.image : background;
  return resolveSource(imgSrc, { maxDim: Math.max(w, h) }).then(function (bgCanvas) {
    if (fit === 'stretch') ctx.drawImage(bgCanvas, 0, 0, w, h);
    else if (fit === 'tile') tile(ctx, bgCanvas, w, h);
    else if (fit === 'contain') drawContain(ctx, bgCanvas, w, h);
    else drawCover(ctx, bgCanvas, w, h);
  });
}

function drawCover(ctx, img, w, h) {
  var iw = img.width, ih = img.height;
  var scale = Math.max(w / iw, h / ih);
  var dw = iw * scale, dh = ih * scale;
  ctx.drawImage(img, (w - dw) / 2, (h - dh) / 2, dw, dh);
}
function drawContain(ctx, img, w, h) {
  var iw = img.width, ih = img.height;
  var scale = Math.min(w / iw, h / ih);
  var dw = iw * scale, dh = ih * scale;
  ctx.drawImage(img, (w - dw) / 2, (h - dh) / 2, dw, dh);
}
function tile(ctx, img, w, h) {
  for (var y = 0; y < h; y += img.height) for (var x = 0; x < w; x += img.width) ctx.drawImage(img, x, y);
}

// replace(src, background, opts) -> canvas: foreground over a new bg.
function replace(src, background, opts) {
  opts = opts || {};
  return resolveSource(src, opts).then(function (working) {
    return remove(working, opts).then(function (fg) {
      var w = fg.width, h = fg.height;
      var out = makeCanvas(w, h);
      var ctx = out.getContext('2d');
      return Promise.resolve(paintBackground(ctx, w, h, background, working)).then(function () {
        ctx.drawImage(fg, 0, 0);
        return out;
      });
    });
  });
}

// ---- output helpers ----
function toDataUrl(canvas, type, quality) { return canvas.toDataURL(type || 'image/png', quality); }
function toBlob(canvas, type, quality) {
  return new Promise(function (resolve) {
    if (canvas.toBlob) canvas.toBlob(function (b) { resolve(b); }, type || 'image/png', quality);
    else resolve(dataUrlToBlob(canvas.toDataURL(type || 'image/png', quality)));
  });
}
function dataUrlToBlob(url) {
  var parts = url.split(','), mime = parts[0].match(/:(.*?);/)[1], bin = atob(parts[1]);
  var arr = new Uint8Array(bin.length);
  for (var i = 0; i < bin.length; i++) arr[i] = bin.charCodeAt(i);
  return new Blob([arr], { type: mime });
}
function download(canvas, filename, type, quality) {
  var a = document.createElement('a');
  a.href = toDataUrl(canvas, type, quality);
  a.download = filename || 'cutout.png';
  document.body.appendChild(a); a.click(); document.body.removeChild(a);
}

// ---- AI hook ----
function useSegmenter(fn) { segmenter = (typeof fn === 'function') ? fn : null; }
function hasSegmenter() { return !!segmenter; }

// ---- portrait helpers (v1.1.0) ----

// Bounding box of non-transparent pixels. Pure. null if fully transparent.
function alphaBounds(alpha, w, h, threshold) {
  threshold = threshold || 0;
  var minX = w, minY = h, maxX = -1, maxY = -1;
  for (var y = 0; y < h; y++) {
    for (var x = 0; x < w; x++) {
      if (alpha[y * w + x] > threshold) {
        if (x < minX) minX = x; if (x > maxX) maxX = x;
        if (y < minY) minY = y; if (y > maxY) maxY = y;
      }
    }
  }
  if (maxX < 0) return null;
  return { x: minX, y: minY, w: maxX - minX + 1, h: maxY - minY + 1 };
}

// Crop a canvas to its non-transparent subject (with optional padding).
// Perfect for turning a cutout into a tidy character portrait.
function trim(canvas, opts) {
  opts = opts || {};
  var ctx = canvas.getContext('2d');
  var img = ctx.getImageData(0, 0, canvas.width, canvas.height);
  var n = canvas.width * canvas.height;
  var alpha = new Uint8ClampedArray(n);
  for (var i = 0; i < n; i++) alpha[i] = img.data[i * 4 + 3];
  var b = alphaBounds(alpha, canvas.width, canvas.height, opts.threshold || 0);
  if (!b) return canvas;
  var pad = opts.padding || 0;
  var x = Math.max(0, b.x - pad), y = Math.max(0, b.y - pad);
  var ww = Math.min(canvas.width - x, b.w + pad * 2);
  var hh = Math.min(canvas.height - y, b.h + pad * 2);
  var out = makeCanvas(ww, hh);
  out.getContext('2d').drawImage(canvas, x, y, ww, hh, 0, 0, ww, hh);
  return out;
}

// Label connected opaque regions (4-connectivity). Pure over an alpha
// array. Returns { labels:Int32Array, comps:[{label,area,x,y,w,h}] }.
function labelComponents(alpha, w, h) {
  var n = w * h;
  var labels = new Int32Array(n);
  var comps = [];
  var next = 1, stack = [];
  for (var p = 0; p < n; p++) {
    if (alpha[p] === 0 || labels[p] !== 0) continue;
    var label = next++, area = 0;
    var minX = w, minY = h, maxX = 0, maxY = 0;
    stack.push(p); labels[p] = label;
    while (stack.length) {
      var idx = stack.pop();
      var x = idx % w, y = (idx - x) / w;
      area++;
      if (x < minX) minX = x; if (x > maxX) maxX = x;
      if (y < minY) minY = y; if (y > maxY) maxY = y;
      if (x > 0 && alpha[idx - 1] > 0 && labels[idx - 1] === 0) { labels[idx - 1] = label; stack.push(idx - 1); }
      if (x < w - 1 && alpha[idx + 1] > 0 && labels[idx + 1] === 0) { labels[idx + 1] = label; stack.push(idx + 1); }
      if (y > 0 && alpha[idx - w] > 0 && labels[idx - w] === 0) { labels[idx - w] = label; stack.push(idx - w); }
      if (y < h - 1 && alpha[idx + w] > 0 && labels[idx + w] === 0) { labels[idx + w] = label; stack.push(idx + w); }
    }
    comps.push({ label: label, area: area, x: minX, y: minY, w: maxX - minX + 1, h: maxY - minY + 1 });
  }
  return { labels: labels, comps: comps };
}

// Keep only the largest opaque blob (default), or, with opts.minArea,
// every blob at least that big -- a despeckle that drops stray pixels
// left over from keying. Returns a new canvas.
function keepLargest(canvas, opts) {
  opts = opts || {};
  var w = canvas.width, h = canvas.height, n = w * h;
  var ctx = canvas.getContext('2d');
  var img = ctx.getImageData(0, 0, w, h);
  var alpha = new Uint8ClampedArray(n);
  for (var i = 0; i < n; i++) alpha[i] = img.data[i * 4 + 3];
  var res = labelComponents(alpha, w, h);
  if (!res.comps.length) return canvas;
  var keep = {};
  if (opts.minArea) {
    for (var c = 0; c < res.comps.length; c++) if (res.comps[c].area >= opts.minArea) keep[res.comps[c].label] = 1;
  } else {
    var best = res.comps[0];
    for (var c2 = 1; c2 < res.comps.length; c2++) if (res.comps[c2].area > best.area) best = res.comps[c2];
    keep[best.label] = 1;
  }
  for (var q = 0; q < n; q++) if (!keep[res.labels[q]]) img.data[q * 4 + 3] = 0;
  var out = makeCanvas(w, h);
  out.getContext('2d').putImageData(img, 0, 0);
  return out;
}

// Split a (background-removed) image into separate cropped object PNGs,
// one per connected region above opts.minArea. Returns Promise of
// [{ canvas, x, y, w, h, area }] sorted largest-first. Set opts.remove
// to false if the source is already transparent.
function extractObjects(src, opts) {
  opts = opts || {};
  var minArea = opts.minArea || 100;
  var prep = (opts.remove === false) ? resolveSource(src, opts) : remove(src, opts);
  return Promise.resolve(prep).then(function (canvas) {
    var w = canvas.width, h = canvas.height, n = w * h;
    var img = canvas.getContext('2d').getImageData(0, 0, w, h);
    var alpha = new Uint8ClampedArray(n);
    for (var i = 0; i < n; i++) alpha[i] = img.data[i * 4 + 3];
    var res = labelComponents(alpha, w, h);
    var out = [];
    for (var c = 0; c < res.comps.length; c++) {
      var comp = res.comps[c];
      if (comp.area < minArea) continue;
      var oc = makeCanvas(comp.w, comp.h);
      var od = oc.getContext('2d').createImageData(comp.w, comp.h);
      for (var yy = 0; yy < comp.h; yy++) {
        for (var xx = 0; xx < comp.w; xx++) {
          var si = ((comp.y + yy) * w + (comp.x + xx));
          if (res.labels[si] !== comp.label) continue;
          var di = (yy * comp.w + xx) * 4;
          od.data[di] = img.data[si * 4];
          od.data[di + 1] = img.data[si * 4 + 1];
          od.data[di + 2] = img.data[si * 4 + 2];
          od.data[di + 3] = img.data[si * 4 + 3];
        }
      }
      oc.getContext('2d').putImageData(od, 0, 0);
      out.push({ canvas: oc, x: comp.x, y: comp.y, w: comp.w, h: comp.h, area: comp.area });
    }
    out.sort(function (a, b) { return b.area - a.area; });
    return out;
  });
}

window.weld.background = {
  remove: remove,
  replace: replace,
  mask: mask,
  maskFromData: maskFromData,
  estimateBgColor: estimateBgColor,
  trim: trim,
  keepLargest: keepLargest,
  despeckle: keepLargest,
  extractObjects: extractObjects,
  alphaBounds: alphaBounds,
  labelComponents: labelComponents,
  fromFile: fromFile,
  fromUrl: fromUrl,
  toDataUrl: toDataUrl,
  toBlob: toBlob,
  download: download,
  useSegmenter: useSegmenter,
  hasSegmenter: hasSegmenter,
  version: '1.1.0'
};

return window.weld.background;
})();
</script>
<script>
(function(){
  function start(){ if (!window.weld || !window.weld.background) { setTimeout(start, 120); return; } build(); }
  function build(){
    var bg = window.weld.background;
    var $ = function(id){ return document.getElementById(id); };
    var toast = function(msg, kind){ if (window.weld && window.weld.toast) window.weld.toast.show(msg, kind || 'success'); };

    var S = {
      src: null, swatches: [], mode: 'global', tol: 0.20, soft: 0.08,
      tidy: true, isolate: false, trim: true,
      portrait: null, scene: 'dusk', sceneImg: null, pos: 'center', scale: 0.92,
      name: 'Aria', line: 'So this is where the story begins...'
    };

    var drop = $('drop'), file = $('file'), work = $('work');
    var srcCanvas = $('srcCanvas'), srcCtx = srcCanvas.getContext('2d');
    var outCanvas = $('outCanvas');

    // ---- load a character image ----
    drop.addEventListener('click', function(){ file.click(); });
    file.addEventListener('change', function(e){ if (e.target.files && e.target.files[0]) loadFile(e.target.files[0]); });
    drop.addEventListener('dragover', function(e){ e.preventDefault(); drop.classList.add('over'); });
    drop.addEventListener('dragleave', function(){ drop.classList.remove('over'); });
    drop.addEventListener('drop', function(e){ e.preventDefault(); drop.classList.remove('over'); if (e.dataTransfer.files && e.dataTransfer.files[0]) loadFile(e.dataTransfer.files[0]); });
    window.addEventListener('paste', function(e){
      var items = (e.clipboardData && e.clipboardData.items) || [];
      for (var i = 0; i < items.length; i++) if (items[i].type && items[i].type.indexOf('image') === 0) { loadFile(items[i].getAsFile()); break; }
    });

    function loadFile(f){
      bg.fromFile(f).then(function(c){
        var maxDim = 1200, scale = Math.min(1, maxDim / Math.max(c.width, c.height));
        var w = Math.round(c.width * scale), h = Math.round(c.height * scale);
        var wc = document.createElement('canvas'); wc.width = w; wc.height = h;
        wc.getContext('2d').drawImage(c, 0, 0, w, h);
        S.src = wc; S.swatches = [];
        srcCanvas.width = w; srcCanvas.height = h; srcCtx.drawImage(wc, 0, 0);
        outCanvas.width = w; outCanvas.height = h;
        drop.classList.add('hide'); work.classList.remove('hide');
        autoDetect(); renderSwatches(); process();
      }).catch(function(err){ alert('Could not read that image: ' + err.message); });
    }

    // ---- eyedropper ----
    function pixelAt(e){
      var r = srcCanvas.getBoundingClientRect();
      var x = Math.floor((e.clientX - r.left) / r.width * srcCanvas.width);
      var y = Math.floor((e.clientY - r.top) / r.height * srcCanvas.height);
      x = Math.max(0, Math.min(srcCanvas.width - 1, x));
      y = Math.max(0, Math.min(srcCanvas.height - 1, y));
      return srcCtx.getImageData(x, y, 1, 1).data;
    }
    function hex(c){ return '#' + [c[0],c[1],c[2]].map(function(v){ return ('0'+v.toString(16)).slice(-2); }).join(''); }
    srcCanvas.addEventListener('mousemove', function(e){
      if (!S.src) return; var p = pixelAt(e);
      $('hover').querySelector('.sw').style.background = hex(p);
      $('hoverHex').textContent = hex(p);
    });
    srcCanvas.addEventListener('click', function(e){ if (!S.src) return; addSwatch(pixelAt(e)); });

    function near(a, b){ return Math.abs(a[0]-b[0]) < 14 && Math.abs(a[1]-b[1]) < 14 && Math.abs(a[2]-b[2]) < 14; }
    function addSwatch(p){
      var c = [p[0], p[1], p[2]];
      for (var i = 0; i < S.swatches.length; i++) if (near(c, S.swatches[i])) return;
      S.swatches.push(c); renderSwatches(); process();
    }
    function autoDetect(){
      try {
        var img = srcCtx.getImageData(0, 0, srcCanvas.width, srcCanvas.height);
        var est = bg.estimateBgColor(img.data, srcCanvas.width, srcCanvas.height);
        if (!S.swatches.length) addSwatch(est);
      } catch (e) {}
    }
    function renderSwatches(){
      var host = $('swatches'); host.innerHTML = '';
      if (!S.swatches.length) { host.innerHTML = '<span class="hoverchip">click the background to pick colours</span>'; return; }
      S.swatches.forEach(function(c, i){
        var chip = document.createElement('span'); chip.className = 'chip';
        chip.innerHTML = '<span class="sw" style="background:' + hex(c) + '"></span>' + hex(c) + ' <span class="x" data-i="' + i + '">x</span>';
        host.appendChild(chip);
      });
      host.querySelectorAll('.x').forEach(function(x){
        x.addEventListener('click', function(){ S.swatches.splice(+x.getAttribute('data-i'), 1); renderSwatches(); process(); });
      });
    }
    $('autoBtn').addEventListener('click', autoDetect);
    $('clearBtn').addEventListener('click', function(){ S.swatches = []; renderSwatches(); process(); });

    // ---- controls ----
    $('modeSeg').addEventListener('click', function(e){
      var b = e.target.closest('button'); if (!b) return;
      S.mode = b.getAttribute('data-mode');
      [].forEach.call(this.children, function(c){ c.classList.toggle('on', c === b); });
      $('modeHint').textContent = S.mode === 'edges' ? 'flood-fills from the borders; keeps a same-coloured subject' : 'erases the picked colours wherever they appear';
      process();
    });
    function bindRange(id, valId, fmt, set){ var el = $(id), o = $(valId); function s(){ o.textContent = fmt(+el.value); set(+el.value); process(); } el.addEventListener('input', s); o.textContent = fmt(+el.value); }
    bindRange('tol', 'tolVal', function(v){ return (v/100).toFixed(2); }, function(v){ S.tol = v/100; });
    bindRange('soft', 'softVal', function(v){ return (v/100).toFixed(2); }, function(v){ S.soft = v/100; });
    $('tidy').addEventListener('change', function(e){ S.tidy = e.target.checked; process(); });
    $('isolate').addEventListener('change', function(e){ S.isolate = e.target.checked; process(); });
    $('trimChk').addEventListener('change', function(e){ S.trim = e.target.checked; process(); });

    // ---- scene controls ----
    $('sceneSeg').addEventListener('click', function(e){
      var b = e.target.closest('button'); if (!b) return;
      S.scene = b.getAttribute('data-scene'); S.sceneImg = null;
      [].forEach.call(this.children, function(c){ c.classList.toggle('on', c === b); });
      renderVN();
    });
    $('sceneBtn').addEventListener('click', function(){ $('sceneFile').click(); });
    $('sceneFile').addEventListener('change', function(e){
      if (!e.target.files || !e.target.files[0]) return;
      bg.fromFile(e.target.files[0]).then(function(c){ S.sceneImg = c; S.scene = 'upload'; [].forEach.call($('sceneSeg').children, function(x){ x.classList.remove('on'); }); renderVN(); });
    });
    $('posSeg').addEventListener('click', function(e){
      var b = e.target.closest('button'); if (!b) return;
      S.pos = b.getAttribute('data-pos');
      [].forEach.call(this.children, function(c){ c.classList.toggle('on', c === b); });
      renderVN();
    });
    bindScale();
    function bindScale(){ var el = $('scale'), o = $('scaleVal'); function s(){ S.scale = +el.value/100; o.textContent = el.value + '%'; renderVN(); } el.addEventListener('input', s); o.textContent = el.value + '%'; }
    $('nameInput').addEventListener('input', function(e){ S.name = e.target.value; renderVN(); });
    $('lineInput').addEventListener('input', function(e){ S.line = e.target.value; renderVN(); });

    $('dlPortrait').addEventListener('click', function(){ if (S.portrait) { bg.download(S.portrait, 'portrait.png'); toast('Portrait downloaded'); } });
    $('dlScene').addEventListener('click', function(){ var vn = $('vnCanvas'); var a = document.createElement('a'); a.href = vn.toDataURL('image/png'); a.download = 'scene.png'; a.click(); });
    $('reset').addEventListener('click', function(){ S.src = null; S.portrait = null; S.swatches = []; work.classList.add('hide'); drop.classList.remove('hide'); file.value = ''; $('dlPortrait').disabled = true; $('dlScene').disabled = true; });

    // ---- run the cut-out pipeline ----
    var timer = null;
    function process(){ if (timer) clearTimeout(timer); timer = setTimeout(run, 110); }
    function run(){
      if (!S.src) return;
      var busy = $('busy'); busy.textContent = 'working...';
      var opts = { keyColors: S.swatches.slice(), tolerance: S.tol, softness: S.soft, contiguous: S.mode === 'edges' };
      bg.remove(S.src, opts).then(function(cut){
        if (S.isolate) cut = bg.keepLargest(cut);
        else if (S.tidy) cut = bg.despeckle(cut, { minArea: Math.max(20, Math.round(cut.width * cut.height * 0.0008)) });
        var portrait = S.trim ? bg.trim(cut, { padding: 6 }) : cut;
        S.portrait = portrait;
        outCanvas.width = portrait.width; outCanvas.height = portrait.height;
        var octx = outCanvas.getContext('2d'); octx.clearRect(0, 0, portrait.width, portrait.height); octx.drawImage(portrait, 0, 0);
        busy.textContent = ''; $('dlPortrait').disabled = false; $('dlScene').disabled = false;
        renderVN();
      }).catch(function(err){ busy.textContent = 'error'; console.error(err); });
    }

    // ---- visual-novel scene preview ----
    function paintScene(ctx, w, h){
      if (S.scene === 'upload' && S.sceneImg){
        var iw = S.sceneImg.width, ih = S.sceneImg.height, sc = Math.max(w/iw, h/ih);
        ctx.drawImage(S.sceneImg, (w-iw*sc)/2, (h-ih*sc)/2, iw*sc, ih*sc); return;
      }
      var g = ctx.createLinearGradient(0, 0, 0, h);
      if (S.scene === 'sky'){ g.addColorStop(0,'#7ec8ff'); g.addColorStop(0.7,'#cfeaff'); g.addColorStop(1,'#eef7ff'); ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
        ctx.fillStyle='rgba(255,250,220,0.9)'; ctx.beginPath(); ctx.arc(w*0.78,h*0.26,46,0,Math.PI*2); ctx.fill(); }
      else if (S.scene === 'room'){ g.addColorStop(0,'#3a2f4a'); g.addColorStop(0.62,'#5a4663'); g.addColorStop(0.62,'#6b4a3a'); g.addColorStop(1,'#42291f'); ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
        ctx.strokeStyle='rgba(0,0,0,0.18)'; ctx.beginPath(); ctx.moveTo(0,h*0.62); ctx.lineTo(w,h*0.62); ctx.stroke(); }
      else { g.addColorStop(0,'#ff9966'); g.addColorStop(0.45,'#ff5e7e'); g.addColorStop(1,'#5b3a8c'); ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
        ctx.fillStyle='rgba(255,220,150,0.85)'; ctx.beginPath(); ctx.arc(w*0.3,h*0.42,60,0,Math.PI*2); ctx.fill(); }
    }
    function roundRect(ctx, x, y, w, h, r){ ctx.beginPath(); ctx.moveTo(x+r,y); ctx.arcTo(x+w,y,x+w,y+h,r); ctx.arcTo(x+w,y+h,x,y+h,r); ctx.arcTo(x,y+h,x,y,r); ctx.arcTo(x,y,x+w,y,r); ctx.closePath(); }
    function wrap(ctx, text, x, y, maxW, lh){ var words=String(text).split(' '), line='', yy=y; for (var i=0;i<words.length;i++){ var t=line+words[i]+' '; if (ctx.measureText(t).width>maxW && i>0){ ctx.fillText(line,x,yy); line=words[i]+' '; yy+=lh; } else line=t; } ctx.fillText(line,x,yy); }
    function renderVN(){
      var vn = $('vnCanvas'), ctx = vn.getContext('2d'), w = vn.width, h = vn.height;
      ctx.clearRect(0,0,w,h); paintScene(ctx, w, h);
      if (S.portrait){
        var p = S.portrait, targetH = h * S.scale, sc = targetH / p.height, dw = p.width * sc, dh = targetH;
        var x = S.pos === 'left' ? w*0.04 : (S.pos === 'right' ? w*0.96 - dw : (w-dw)/2);
        ctx.save(); ctx.shadowColor='rgba(0,0,0,0.35)'; ctx.shadowBlur=18; ctx.shadowOffsetY=6;
        ctx.drawImage(p, x, h - dh, dw, dh); ctx.restore();
      }
      // dialogue box
      var bx=18, bw=w-36, bh=92, by=h-bh-14;
      ctx.fillStyle='rgba(8,12,22,0.82)'; roundRect(ctx,bx,by,bw,bh,12); ctx.fill();
      ctx.strokeStyle='rgba(68,147,248,0.7)'; ctx.lineWidth=1.5; roundRect(ctx,bx,by,bw,bh,12); ctx.stroke();
      if (S.name){ ctx.fillStyle=S.scene==='sky'?'#2563eb':'#4493f8'; roundRect(ctx,bx+14,by-16,Math.max(70,ctx.measureText(S.name).width+34),28,8); ctx.fill();
        ctx.fillStyle='#fff'; ctx.font='600 14px DM Sans, sans-serif'; ctx.textBaseline='middle'; ctx.fillText(S.name,bx+28,by-2); }
      ctx.fillStyle='#e6edf3'; ctx.font='15px DM Sans, sans-serif'; ctx.textBaseline='top'; wrap(ctx, S.line, bx+20, by+18, bw-40, 22);
    }
    renderVN();
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', start);
  else start();
})();
</script>
