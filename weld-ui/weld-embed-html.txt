<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.embed<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-embed-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.embed</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">Local semantic embeddings for search and memory. Runs a transformer in a Web Worker, caches vectors in IndexedDB, and ranks text by meaning &mdash; with a lexical fallback so search always works.</p>
</div>
<h2>Install</h2>
<p>Import the plugin once. It stashes its API on <code>window.weld.embed</code>. No other plugins are required. The model loads lazily from a CDN inside the Perchance frame on first use; if it can't load, weld.embed falls back to a deterministic lexical embedding so search keeps working.</p>
<pre data-codeid="install"></pre>

<h2>Quick start</h2>
<p>Rank a list of items against a query by meaning. <code>rank()</code> embeds everything (cached), scores by cosine similarity, and returns the top matches.</p>
<pre data-codeid="quick"></pre>

<h2>rank(query, items, opts)</h2>
<div class="sig" data-codeid="sig_rank"></div>
<p>Returns a promise for <code>\[\{ item, index, score \}\]</code> sorted high-to-low. Items may be plain strings or objects; for objects, set <code>opts.key</code> to the text field.</p>
<table>
<tr><th>opt</th><th>default</th><th>meaning</th></tr>
<tr><td><code>topK</code></td><td>5</td><td>how many matches to return</td></tr>
<tr><td><code>minScore</code></td><td>0</td><td>drop matches below this cosine score</td></tr>
<tr><td><code>key</code></td><td>'text'</td><td>field to read when items are objects</td></tr>
</table>

<h2>embed / embedBatch / similarity</h2>
<div class="sig" data-codeid="sig_embed"></div>
<div class="sig" data-codeid="sig_batch"></div>
<div class="sig" data-codeid="sig_sim"></div>
<p>Vectors are L2-normalized at the source, so <code>similarity</code> is a plain dot product (equal to cosine). <code>embedBatch</code> checks the in-memory and IndexedDB caches first and only computes the misses, in one worker round-trip.</p>
<pre data-codeid="batch_ex"></pre>

<h2>Engine &amp; fallback</h2>
<div class="sig" data-codeid="sig_info"></div>
<p><code>info().backend</code> is <code>'worker'</code> (the real transformer model), <code>'fallback'</code> (lexical hash embedding &mdash; works offline but has no synonymy), or <code>'custom'</code> (your own embedder). Always check it when ranking quality matters.</p>
<div class="note">The model runs in a Web Worker, so embedding a large batch never freezes the UI. Vectors are cached in IndexedDB keyed by model + text, so repeated text is free after the first pass.</div>

<h2>Bring your own embedder</h2>
<div class="sig" data-codeid="sig_use"></div>
<p>Already have an embedding source (a different model, an API)? Register it and weld.embed routes <code>embed</code>/<code>rank</code> through it, keeping the cache and similarity helpers.</p>
<pre data-codeid="use_ex"></pre>

<h2>Powering semantic memory</h2>
<p>Attach weld.embed to <code>weld.memory</code> and recall ranks by meaning instead of keyword overlap:</p>
<pre data-codeid="mem_ex"></pre>

<h2>Cache control</h2>
<div class="sig" data-codeid="sig_clear"></div>
<div class="sig" data-codeid="sig_size"></div>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-embed = \{import:weld-embed-plugin\}',
  'quick': 'var items = \[\'The dog is named Max.\', \'The capital of France is Paris.\'];\nvar hits = await weld.embed.rank(\'tell me about the pet\', items, \{ topK: 1 \});\nhits\[0].item;   // \'The dog is named Max.\'\nhits\[0].score;  // cosine similarity, 0..1',
  'sig_rank': 'weld.embed.rank(query, items, opts) -> Promise<\[\{ item, index, score \}]>',
  'sig_embed': 'weld.embed.embed(text) -> Promise<number\[] | null>',
  'sig_batch': 'weld.embed.embedBatch(texts) -> Promise<(number\[]|null)\[]>',
  'sig_sim': 'weld.embed.similarity(a, b) -> number   // cosine (vectors are normalized)',
  'batch_ex': 'var vs = await weld.embed.embedBatch(\[\'a cat on a mat\', \'a kitten on a rug\']);\nweld.embed.similarity(vs\[0], vs\[1]);   // high -- semantically close',
  'sig_info': 'weld.embed.info() -> \{ model, backend, ready, dim \}',
  'sig_use': 'weld.embed.useEmbedder(fn)   //  fn(texts) -> vectors',
  'use_ex': 'weld.embed.useEmbedder(async function(texts)\{\n  var r = await myApi.embed(texts);\n  return r.vectors;   // array of number\[]\n\});',
  'mem_ex': 'weld.memory.attach(\{ embed: weld.embed \});\nvar context = await weld.memory.injectAsync(userMessage);  // ranked by meaning',
  'sig_clear': 'weld.embed.clearCache() -> Promise<boolean>',
  'sig_size': 'weld.embed.cacheSize() -> number'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.embed) return window.weld.embed;

// -----------------------------------------------------------------
// weld.embed: turn text into vectors, in the browser, with no server.
//
// Two serious community chat apps independently reached for the same
// thing -- transformers.js feature-extraction in a Web Worker, cached
// in IndexedDB -- to get semantic memory and lore retrieval. This packs
// that pattern into one plugin so weld.memory and weld.lorebook can
// rank by meaning instead of keyword overlap.
//
//   init(opts)          boot the worker + model (lazy; rank/embed call it)
//   embed(text)         -> Promise<vector | null>
//   embedBatch(texts)   -> Promise<(vector|null)[]>  (cached + batched)
//   similarity(a, b)    cosine (vectors are L2-normalized, so a dot)
//   rank(query, items, opts) -> Promise<[{ item, index, score }]> top-k
//   useEmbedder(fn)     swap in your own async embedder (or for testing)
//   clearCache/cacheSize/info/isReady
//
// Robustness: the real model loads from a CDN inside the Perchance
// frame. If that fails (offline, blocked, no WASM), weld.embed falls
// back to a deterministic lexical hash embedding -- lower quality, but
// rank() still returns sensible keyword-overlap results instead of
// throwing. info().backend tells you which engine is live.
// -----------------------------------------------------------------

var DEFAULTS = {
  model: 'Xenova/all-MiniLM-L6-v2', // small, 384-dim, widely cached
  fallbackDim: 256,
  cdn: 'https://cdn.jsdelivr.net/npm/@xenova/transformers@2.17.2',
  timeoutMs: 12000,
  dbName: 'weld-embed',
  store: 'vectors'
};

var cfg = {};
for (var k in DEFAULTS) cfg[k] = DEFAULTS[k];

var worker = null;
var reqId = 0;
var pending = {};        // id -> { resolve, reject, timer }
var ready = false;
var booting = null;      // Promise while init is in flight
var backend = 'none';    // 'worker' | 'fallback' | 'custom'
var customEmbedder = null;
var modelDim = 0;
var db = null;
var mem = {};            // in-memory vector cache (key -> vector)

function cacheKey(text) { return (backend === 'fallback' ? 'lex' : cfg.model) + '|' + text; }

// ---- IndexedDB cache (best-effort; never fatal) ----
function openDb() {
  return new Promise(function (resolve) {
    if (db) { resolve(db); return; }
    if (typeof indexedDB === 'undefined') { resolve(null); return; }
    var req = indexedDB.open(cfg.dbName, 1);
    req.onupgradeneeded = function (e) {
      var d = e.target.result;
      if (!d.objectStoreNames.contains(cfg.store)) d.createObjectStore(cfg.store, { keyPath: 'key' });
    };
    req.onsuccess = function (e) { db = e.target.result; resolve(db); };
    req.onerror = function () { resolve(null); };
  });
}

function cacheGetMany(keys) {
  return openDb().then(function (d) {
    if (!d) return {};
    return new Promise(function (resolve) {
      var out = {}, left = keys.length;
      if (!left) { resolve(out); return; }
      var tx = d.transaction([cfg.store], 'readonly');
      var store = tx.objectStore(cfg.store);
      keys.forEach(function (key) {
        var r = store.get(key);
        r.onsuccess = function () { if (r.result) out[key] = r.result.vector; if (--left === 0) resolve(out); };
        r.onerror = function () { if (--left === 0) resolve(out); };
      });
    });
  });
}

function cachePut(key, vector) {
  mem[key] = vector;
  openDb().then(function (d) {
    if (!d) return;
    try {
      var tx = d.transaction([cfg.store], 'readwrite');
      tx.objectStore(cfg.store).put({ key: key, vector: vector });
    } catch (e) { /* ignore */ }
  });
}

// ---- the worker source (built so the Perchance scanner sees no traps) ----
// No doubled square or curly brackets, no import-directive token; object literals keep spaces.
function workerSource() {
  var L = [];
  L.push("var pipe = null;");
  L.push("var CDN = self.__cdn;");
  L.push("function reply(id, ok, payload){ self.postMessage(Object.assign({ id: id, ok: ok }, payload)); }");
  L.push("self.onmessage = async function(e){");
  L.push("  var msg = e.data || {};");
  L.push("  try {");
  L.push("    if (msg.type === 'init') {");
  L.push("      if (!pipe) {");
  L.push("        importScripts(msg.cdn || CDN);");
  L.push("        var T = self.transformers || self.Transformers;");
  L.push("        if (T && T.env) { T.env.allowLocalModels = false; }");
  L.push("        pipe = await T.pipeline('feature-extraction', msg.model);");
  L.push("      }");
  L.push("      reply(msg.id, true, { dim: 0 });");
  L.push("      return;");
  L.push("    }");
  L.push("    if (msg.type === 'embed') {");
  L.push("      if (!pipe) { reply(msg.id, false, { error: 'model not initialized' }); return; }");
  L.push("      var out = await pipe(msg.texts, { pooling: 'mean', normalize: true });");
  L.push("      var data = out.data;");
  L.push("      var dims = out.dims || [msg.texts.length, data.length / msg.texts.length];");
  L.push("      var dim = dims[dims.length - 1];");
  L.push("      var vecs = [];");
  L.push("      for (var i = 0; i < msg.texts.length; i++) {");
  L.push("        var start = i * dim, v = new Array(dim);");
  L.push("        for (var j = 0; j < dim; j++) { v[j] = data[start + j]; }");
  L.push("        vecs.push(v);");
  L.push("      }");
  L.push("      reply(msg.id, true, { vectors: vecs, dim: dim });");
  L.push("      return;");
  L.push("    }");
  L.push("  } catch (err) { reply(msg.id, false, { error: String(err && err.message || err) }); }");
  L.push("};");
  return L.join('\n');
}

function startWorker() {
  if (typeof Worker === 'undefined' || typeof Blob === 'undefined') return null;
  // Prepend the CDN as a worker global so importScripts can default to it.
  var src = "self.__cdn=" + JSON.stringify(cfg.cdn) + ";\n" + workerSource();
  var url = URL.createObjectURL(new Blob([src], { type: 'text/javascript' }));
  var w = new Worker(url);
  w.onmessage = function (e) {
    var m = e.data || {};
    var p = pending[m.id];
    if (!p) return;
    delete pending[m.id];
    if (p.timer) clearTimeout(p.timer);
    if (m.ok) p.resolve(m);
    else p.reject(new Error(m.error || 'worker error'));
  };
  w.onerror = function () { /* surfaced per-request via timeout */ };
  return w;
}

function call(type, payload) {
  return new Promise(function (resolve, reject) {
    var id = ++reqId;
    pending[id] = {
      resolve: resolve, reject: reject,
      timer: setTimeout(function () {
        if (pending[id]) { delete pending[id]; reject(new Error('weld.embed: worker timeout')); }
      }, cfg.timeoutMs)
    };
    var msg = { id: id, type: type };
    for (var key in payload) msg[key] = payload[key];
    worker.postMessage(msg);
  });
}

// ---- deterministic lexical fallback (no model, no network) ----
var STOP = { the:1,a:1,an:1,and:1,or:1,but:1,is:1,are:1,was:1,were:1,to:1,of:1,
  in:1,on:1,at:1,for:1,with:1,it:1,this:1,that:1,as:1,by:1 };
function tokens(text) {
  return String(text || '').toLowerCase().replace(/[^a-z0-9\u00c0-\u024f' ]+/g, ' ')
    .split(/\s+/).filter(function (w) { return w.length > 1 && !STOP[w]; });
}
function hashStr(s) {
  var h = 2166136261 >>> 0;
  for (var i = 0; i < s.length; i++) { h ^= s.charCodeAt(i); h = Math.imul(h, 16777619); }
  return h >>> 0;
}
function lexEmbed(text) {
  var dim = cfg.fallbackDim, v = new Array(dim);
  for (var i = 0; i < dim; i++) v[i] = 0;
  var toks = tokens(text);
  for (var t = 0; t < toks.length; t++) {
    var tok = toks[t];
    // two hashed projections per token soften collisions
    v[hashStr(tok) % dim] += 1;
    v[hashStr('#' + tok) % dim] += 0.5;
  }
  // L2 normalize so cosine == dot
  var norm = 0; for (var n = 0; n < dim; n++) norm += v[n] * v[n];
  norm = Math.sqrt(norm) || 1;
  for (var m2 = 0; m2 < dim; m2++) v[m2] = v[m2] / norm;
  return v;
}

// ---- init / engine selection ----
function init(opts) {
  if (opts) for (var key in opts) if (opts[key] != null) cfg[key] = opts[key];
  if (ready) return Promise.resolve(info());
  if (booting) return booting;

  if (customEmbedder) { backend = 'custom'; ready = true; return Promise.resolve(info()); }

  booting = new Promise(function (resolve) {
    worker = startWorker();
    if (!worker) { backend = 'fallback'; ready = true; resolve(info()); return; }
    call('init', { model: cfg.model, cdn: cfg.cdn }).then(function () {
      backend = 'worker'; ready = true; resolve(info());
    }, function () {
      // Worker present but model failed to load -> lexical fallback.
      try { worker.terminate(); } catch (e) {}
      worker = null; backend = 'fallback'; ready = true; resolve(info());
    });
  });
  return booting;
}

// ---- embedding (cache-aware, batched) ----
function embedBatch(texts) {
  if (!Array.isArray(texts) || !texts.length) return Promise.resolve([]);
  return init().then(function () {
    var keys = texts.map(cacheKey);
    // in-memory first
    var result = new Array(texts.length);
    var missIdx = [], missTexts = [], missKeys = [];
    for (var i = 0; i < texts.length; i++) {
      if (mem[keys[i]]) result[i] = mem[keys[i]];
      else { missIdx.push(i); missTexts.push(texts[i]); missKeys.push(keys[i]); }
    }
    if (!missIdx.length) return result;

    return cacheGetMany(missKeys).then(function (fromDb) {
      var stillIdx = [], stillTexts = [], stillKeys = [];
      for (var j = 0; j < missIdx.length; j++) {
        var key = missKeys[j];
        if (fromDb[key]) { result[missIdx[j]] = fromDb[key]; mem[key] = fromDb[key]; }
        else { stillIdx.push(missIdx[j]); stillTexts.push(missTexts[j]); stillKeys.push(key); }
      }
      if (!stillIdx.length) return result;

      // compute the remainder
      var compute;
      if (backend === 'custom') {
        compute = Promise.resolve(customEmbedder(stillTexts)).then(function (vs) { return { vectors: vs }; });
      } else if (backend === 'worker') {
        compute = call('embed', { texts: stillTexts }).then(function (m) {
          if (m.dim) modelDim = m.dim;
          return { vectors: m.vectors };
        }, function () {
          // mid-session worker failure: fall back lexically for this batch
          backend = 'fallback';
          return { vectors: stillTexts.map(lexEmbed) };
        });
      } else {
        compute = Promise.resolve({ vectors: stillTexts.map(lexEmbed) });
      }

      return compute.then(function (r) {
        var vs = r.vectors || [];
        for (var z = 0; z < stillIdx.length; z++) {
          var vec = vs[z] || null;
          result[stillIdx[z]] = vec;
          if (vec) cachePut(stillKeys[z], vec);
        }
        return result;
      });
    });
  });
}

function embed(text) { return embedBatch([text]).then(function (a) { return a[0]; }); }

// ---- math ----
function similarity(a, b) {
  if (!a || !b || a.length !== b.length) return 0;
  var s = 0; for (var i = 0; i < a.length; i++) s += a[i] * b[i];
  return s; // vectors are normalized at the source -> cosine
}

// rank(query, items, opts) -> top-k by similarity.
// items: string[] | object[]; opts: { topK, minScore, key }
function rank(query, items, opts) {
  opts = opts || {};
  var key = opts.key || 'text';
  var topK = opts.topK || 5;
  var minScore = opts.minScore != null ? opts.minScore : 0;
  if (!items || !items.length) return Promise.resolve([]);
  var texts = items.map(function (it) {
    return (typeof it === 'string') ? it : String((it && it[key]) != null ? it[key] : '');
  });
  return embedBatch([query].concat(texts)).then(function (vecs) {
    var qv = vecs[0];
    if (!qv) return [];
    var scored = [];
    for (var i = 0; i < items.length; i++) {
      var v = vecs[i + 1];
      if (!v) continue;
      var score = similarity(qv, v);
      if (score >= minScore) scored.push({ item: items[i], index: i, score: score });
    }
    scored.sort(function (a, b) { return b.score - a.score; });
    return scored.slice(0, topK);
  });
}

function useEmbedder(fn) {
  customEmbedder = (typeof fn === 'function') ? fn : null;
  if (customEmbedder) { backend = 'custom'; ready = true; }
}

function clearCache() {
  mem = {};
  return openDb().then(function (d) {
    if (!d) return false;
    return new Promise(function (resolve) {
      try {
        var tx = d.transaction([cfg.store], 'readwrite');
        var r = tx.objectStore(cfg.store).clear();
        r.onsuccess = function () { resolve(true); };
        r.onerror = function () { resolve(false); };
      } catch (e) { resolve(false); }
    });
  });
}

function cacheSize() { return Object.keys(mem).length; }
function isReady() { return ready; }
function info() { return { model: cfg.model, backend: backend, ready: ready, dim: modelDim || cfg.fallbackDim }; }

window.weld.embed = {
  init: init,
  embed: embed,
  embedBatch: embedBatch,
  similarity: similarity,
  rank: rank,
  search: rank,
  useEmbedder: useEmbedder,
  clearCache: clearCache,
  cacheSize: cacheSize,
  isReady: isReady,
  info: info,
  version: '1.0.0'
};

return window.weld.embed;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
