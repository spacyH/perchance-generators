$meta
  title       = weld.safe examples
  description = Interactive demo of weld.safe -- paste plugin code and see the Perchance brace-interception traps with line numbers, explanations, and fixes. Includes a string escaper and a safe-token builder.
  tags        = weld, safe, brace, perchance, example, plugin

  weldSafe = {import:weld-safe-plugin}
