$meta
  title       = weld.summarize examples
  description = Interactive demo of weld.summarize. Keep a long chat inside the context window: recent turns stay verbatim while older ones fold into a running summary.
  tags        = weld, summarize, example, demo, plugin

  weldSummarize    = {import:weld-summarize-plugin}
