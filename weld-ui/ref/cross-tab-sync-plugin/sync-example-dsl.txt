$meta
  title       = Cross-Tab Sync — Examples
  description = Interactive demos for cross-tab-sync-plugin. Open this page in multiple tabs to see real-time peer count, shared state, broadcast messaging, persistent counters, and reactive state primitives.
  tags        = plugin, example, sync, multiplayer, demo

sync = {import:cross-tab-sync-plugin}
