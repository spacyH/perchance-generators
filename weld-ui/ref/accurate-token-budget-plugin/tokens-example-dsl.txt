$meta
  title       = Token Budget Plugin — Examples
  description = Interactive demos for accurate-token-budget-plugin. Count tokens exactly using the DeepSeek-R1 tokenizer, truncate text to fit budgets, chunk long documents, and plan chat context windows.
  tags        = plugin, example, tokens, demo

tokens = {import:accurate-token-budget-plugin}
