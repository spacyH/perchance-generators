$meta
  title = weld.reactions examples
  description = Interactive demo of weld.reactions -- aggregate emoji reactions on any item id, with per-user toggle, counts, and change events. Backend-agnostic; renders nothing itself.
  tags = weld, reactions, emoji, chat, social, example, plugin

  weldReactions = {import:weld-reactions-plugin}
