<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.changelog{margin:14px 0;padding:14px 16px;background:var(--faint);border:1px solid var(--border);border-radius:6px;}
.changelog .cl-ver{font:700 13px var(--mono);color:var(--accent);margin:10px 0 4px;}
.changelog .cl-ver:first-child{margin-top:0;}
.changelog ul{margin:0 0 4px;padding-left:18px;}
.changelog li{font-size:13px;color:#c9d1d9;line-height:1.5;margin:2px 0;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.rules<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-rules-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.rules</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">A small declarative rules engine: when a metric crosses a threshold, do something. An app registers its own metric getters and an action handler; rules are plain data (when/op/value -> then) so they can be authored, saved, and edited without code. Adds a hold duration (condition must stay true N seconds before firing) and a cooldown (min gap between re-fires). Drive with tick(elapsedSec) or evaluate once. Backend-agnostic; bakes in no particular metrics.</p>
</div>
<h2>Reactive automation for your own signals</h2>
<p>A rules engine answers one question: <em>when</em> should something happen? weld.rules keeps that logic and nothing else &mdash; the app owns the signals and the actions. You register metric getters and an action handler, declare rules as plain data, and tick the engine from your loop. Because rules are just data, they can be authored, saved, edited, and imported without touching code.</p>
<pre data-codeid="install"></pre>
<p>It bakes in <strong>no</strong> particular metrics or actions. It compares numbers and dispatches whatever you wrote &mdash; so the same engine drives &ldquo;summarize when the context gets long,&rdquo; &ldquo;autosave after idle,&rdquo; or &ldquo;switch model after two failures.&rdquo;</p>

<h2>Setup: metrics &amp; actions</h2>
<div class="sig" data-codeid="sig_metric"></div>
<p><code>metric(name, getter)</code> registers a signal &mdash; <code>getter()</code> returns the current value each time the engine needs it. <code>onAction(fn)</code> registers a handler that receives <code>(action, rule)</code> when a rule fires; a rule can also carry its own <code>then.run</code> callback. Both are how the engine reaches back into your app.</p>
<pre data-codeid="setup_ex"></pre>

<h2>Rules are plain data</h2>
<div class="sig" data-codeid="sig_rule"></div>
<p>A rule is <code>\{ when: \{ metric, op, value \}, then, durationSec, cooldownSec \}</code>. <code>op</code> is one of <code>&lt; &gt; &lt;= &gt;= == !=</code>. <code>then</code> is any action object you define. The two timing controls are what make rules safe to run every tick:</p>
<ul>
<li><code>durationSec</code> &mdash; the condition must stay true this long before firing (debounce). A condition going false resets the hold. Default 0 (fire immediately).</li>
<li><code>cooldownSec</code> &mdash; minimum gap between re-fires. Default 0 (no throttle).</li>
</ul>
<pre data-codeid="rule_ex"></pre>

<h2>Driving the engine</h2>
<div class="sig" data-codeid="sig_tick"></div>
<p><code>tick(elapsedSec)</code> advances the engine &mdash; call it from your loop (e.g. once a second) with the seconds elapsed. It evaluates every enabled rule, manages hold and cooldown, fires matching actions, and returns the rules that fired. <code>evaluateNow()</code> fires anything true right now (ignoring <code>durationSec</code> but still respecting cooldown) &mdash; handy for a one-shot check after an event. <code>reset()</code> clears hold/cooldown state without removing rules, e.g. on a session switch.</p>
<pre data-codeid="tick_ex"></pre>

<h2>Managing rules</h2>
<div class="sig" data-codeid="sig_crud"></div>
<p>Add, update, toggle, and remove rules at runtime; <code>list()</code> returns them as plain data and <code>snapshot()</code> / <code>restore()</code> round-trip them for persistence (pair with <code>weld.persist</code> or <code>weld.kv</code>).</p>
<div class="note">Pairs across the suite: drive metrics from <code>weld.tokens</code> (context length), <code>weld.history</code> (idle/last-save), or <code>weld.stream</code> (failure count); fire actions into <code>weld.summarize</code>, <code>weld.toast</code>, or <code>weld.session</code>. The engine is the connective tissue, not another backend.</div>
<h2>Changelog</h2>
<div class="changelog">
<p class="cl-ver">v1.0.0</p>
<ul>
<li>Initial release: declarative when-X-do-Y engine; metric/onAction registration, durationSec hold + cooldownSec throttle, tick/evaluateNow/reset, CRUD + snapshot/restore.</li>
</ul>
</div>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-rules = \{import:weld-rules-plugin\}',
  'sig_metric': 'r.metric(name, getter)   //  r.onAction(fn) -> unsubscribe   //  fn(action, rule)',
  'setup_ex': 'var r = weld.rules.create();\nr.metric(\'tokens\', function () \{ return weld.tokens.count(buffer); \});\nr.metric(\'idleSec\', function () \{ return (Date.now() - lastInput) / 1000; \});\nr.onAction(function (action, rule) \{\n  if (action.type === \'summarize\') doSummary();\n  if (action.type === \'autosave\')  hist.save();\n\});',
  'sig_rule': 'r.add(\{ when: \{ metric, op, value \}, then, durationSec?, cooldownSec? \}) -> rule',
  'rule_ex': 'r.add(\{ when: \{ metric: \'tokens\', op: \'>\', value: 3000 \},\n        then: \{ type: \'summarize\' \}, cooldownSec: 60 \});\n\nr.add(\{ when: \{ metric: \'idleSec\', op: \'>=\', value: 30 \},\n        then: \{ type: \'autosave\' \}, durationSec: 0, cooldownSec: 30 \});',
  'sig_tick': 'r.tick(elapsedSec) -> firedRules\[]   //  r.evaluateNow() -> firedRules\[]   //  r.reset()',
  'tick_ex': '// drive it from your loop, 1-second frames:\nsetInterval(function () \{ r.tick(1); \}, 1000);\n\n// or check once, right after an event:\nweld.stream.generate(opts).catch(function () \{ failures++; r.evaluateNow(); \});',
  'sig_crud': 'r.add / r.update(id, patch) / r.toggle(id, on?) / r.remove(id) / r.list() / r.snapshot() / r.restore(list)'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.rules) return window.weld.rules;

// -----------------------------------------------------------------
// weld.rules: reactive "when X, do Y" for app-supplied signals.
//
// The app owns the signals and the actions; weld.rules owns the logic of
// deciding WHEN to act. You register metric getters and an action handler,
// declare rules as plain data, and tick the engine from your loop:
//
//   var r = weld.rules.create();
//   r.metric('tokens', function () { return weld.tokens.count(buffer); });
//   r.metric('idleSec', function () { return (Date.now() - lastInput) / 1000; });
//   r.onAction(function (action, rule) {
//     if (action.type === 'summarize') doSummary();
//     if (action.type === 'autosave')  hist.save();
//   });
//   r.add({ when: { metric: 'tokens', op: '>', value: 3000 },
//           then: { type: 'summarize' }, cooldownSec: 60 });
//   r.add({ when: { metric: 'idleSec', op: '>=', value: 30 },
//           then: { type: 'autosave' }, durationSec: 0, cooldownSec: 30 });
//   setInterval(function () { r.tick(1); }, 1000);   // 1s frames
//
// A RULE is plain data so it can be saved/edited/imported:
//   { id, enabled, when: { metric, op, value }, then: <action>,
//     durationSec, cooldownSec }
//   op is one of < > <= >= == != ; durationSec = the condition must hold
//   this long before firing (debounce); cooldownSec = min gap between
//   re-fires. Both default to 0.
//
// The engine bakes in NO metrics and NO actions -- it compares numbers and
// dispatches whatever 'then' you wrote to your handler. That keeps it
// backend-agnostic and reusable across any weld app.
// -----------------------------------------------------------------

var OPS = {
  '<':  function (a, b) { return a <  b; },
  '>':  function (a, b) { return a >  b; },
  '<=': function (a, b) { return a <= b; },
  '>=': function (a, b) { return a >= b; },
  '==': function (a, b) { return a === b; },
  '!=': function (a, b) { return a !== b; }
};

var idSeq = 0;
function uid() { idSeq++; return 'r' + Date.now().toString(36) + '_' + idSeq.toString(36); }

function createEngine(opts) {
  opts = opts || {};
  var metrics = {};        // name -> getter function
  var actionHandlers = new Array();
  var rules = new Array();
  var ruleState = {};      // id -> { heldSec, lastFiredAt }
  var clock = 0;           // absolute seconds, accumulated across ticks

  // ---- metrics ----
  // register(name, getter): getter() -> number (or anything == comparable).
  function metric(name, getter) {
    if (!name || typeof getter !== 'function') return;
    Reflect.set(metrics, String(name), getter);
    return engine;
  }
  function readMetric(name) {
    var g = Reflect.get(metrics, String(name));
    if (typeof g !== 'function') return undefined;
    try { return g(); } catch (e) { return undefined; }
  }
  function metricNames() { return Object.keys(metrics); }

  // ---- action handlers ----
  function onAction(fn) {
    if (typeof fn !== 'function') return function () {};
    actionHandlers.push(fn);
    return function () {
      var ix = actionHandlers.indexOf(fn);
      if (ix !== -1) actionHandlers.splice(ix, 1);
    };
  }
  function dispatch(action, rule) {
    for (var i = 0; i < actionHandlers.length; i++) {
      try { Reflect.get(actionHandlers, i)(action, rule); } catch (e) { /* swallow */ }
    }
    // a rule may also carry its own callback in `then.run`
    if (rule && rule.then && typeof rule.then.run === 'function') {
      try { rule.then.run(rule); } catch (e) { /* swallow */ }
    }
  }

  // ---- rule normalization (plain data in, valid rule out) ----
  function normalize(r) {
    r = r || {};
    var when = r.when || r.condition || {};
    var op = Reflect.get(OPS, when.op) ? when.op : '>';
    return {
      id: r.id || uid(),
      enabled: r.enabled !== false,
      when: {
        metric: String(when.metric || ''),
        op: op,
        value: when.value
      },
      then: r.then || r.action || { type: 'noop' },
      durationSec: numOr(r.durationSec, 0),
      cooldownSec: numOr(r.cooldownSec, 0),
      meta: r.meta || null
    };
  }
  function numOr(v, d) { var n = Number(v); return isFinite(n) && n >= 0 ? n : d; }

  // ---- CRUD on rules ----
  function add(r) {
    var rule = normalize(r);
    rules.push(rule);
    Reflect.set(ruleState, rule.id, { heldSec: 0, lastFiredAt: -Infinity });
    return rule;
  }
  function addAll(list) { return (list || new Array()).map(add); }
  function get(id) { for (var i = 0; i < rules.length; i++) { var ru = Reflect.get(rules, i); if (ru.id === id) return ru; } return null; }
  function update(id, patch) {
    var rule = get(id);
    if (!rule) return null;
    var merged = normalize(Object.assign({}, rule, patch, { id: id }));
    for (var i = 0; i < rules.length; i++) { if (Reflect.get(rules, i).id === id) { Reflect.set(rules, i, merged); break; } }
    return merged;
  }
  function remove(id) {
    for (var i = 0; i < rules.length; i++) {
      if (Reflect.get(rules, i).id === id) { rules.splice(i, 1); Reflect.deleteProperty(ruleState, id); return true; }
    }
    return false;
  }
  function toggle(id, on) {
    var rule = get(id);
    if (!rule) return null;
    rule.enabled = (on === undefined) ? !rule.enabled : (on === true);
    return rule.enabled;
  }
  function list() { return rules.map(function (r) { return Object.assign({}, r); }); }
  function clear() { rules = new Array(); ruleState = {}; }

  // ---- evaluation ----
  // evalRule(rule): is the condition currently true? (no side effects)
  function evalRule(rule) {
    var fn = Reflect.get(OPS, rule.when.op);
    if (!fn) return false;
    var actual = readMetric(rule.when.metric);
    if (actual === undefined) return false;
    return fn(actual, rule.when.value);
  }

  // tick(elapsedSec): advance the engine by elapsedSec seconds. For each
  // enabled rule, if its condition holds, accumulate hold time; once the
  // hold reaches durationSec AND the cooldown has elapsed since the last
  // fire, dispatch the action. A condition going false resets the hold.
  // Returns the rules that fired this tick.
  function tick(elapsedSec) {
    var dt = numOr(elapsedSec, 0);
    clock += dt;
    var fired = new Array();
    for (var i = 0; i < rules.length; i++) {
      var rule = Reflect.get(rules, i);
      if (!rule.enabled) continue;
      var rs = Reflect.get(ruleState, rule.id) || { heldSec: 0, lastFiredAt: -Infinity };
      if (evalRule(rule)) {
        rs.heldSec += dt;
        if (rs.heldSec >= rule.durationSec) {
          var sinceFire = clock - rs.lastFiredAt;
          if (rs.lastFiredAt === -Infinity || sinceFire >= rule.cooldownSec) {
            rs.lastFiredAt = clock;
            rs.heldSec = 0;
            dispatch(rule.then, rule);
            fired.push(rule);
          }
        }
      } else {
        rs.heldSec = 0;
      }
      Reflect.set(ruleState, rule.id, rs);
    }
    return fired;
  }

  // evaluateNow(): fire any rules whose condition is true RIGHT NOW,
  // ignoring durationSec but still respecting cooldown. Handy for one-shot
  // checks outside a tick loop (e.g. right after an event).
  function evaluateNow() {
    var fired = new Array();
    for (var i = 0; i < rules.length; i++) {
      var rule = Reflect.get(rules, i);
      if (!rule.enabled) continue;
      var rs = Reflect.get(ruleState, rule.id) || { heldSec: 0, lastFiredAt: -Infinity };
      if (evalRule(rule)) {
        var sinceFire = clock - rs.lastFiredAt;
        if (rs.lastFiredAt === -Infinity || sinceFire >= rule.cooldownSec) {
          rs.lastFiredAt = clock;
          rs.heldSec = 0;
          dispatch(rule.then, rule);
          fired.push(rule);
          Reflect.set(ruleState, rule.id, rs);
        }
      }
    }
    return fired;
  }

  // reset(): clear hold/cooldown state without removing rules (e.g. on a
  // session switch) so cooldowns don't carry across contexts.
  function reset() {
    clock = 0;
    for (var i = 0; i < rules.length; i++) {
      Reflect.set(ruleState, Reflect.get(rules, i).id, { heldSec: 0, lastFiredAt: -Infinity });
    }
  }

  // snapshot(): the rules as plain data (for saving). restore(list) loads.
  function snapshot() { return list(); }
  function restore(saved) { clear(); addAll(saved || new Array()); }

  var engine = {
    metric: metric, metricNames: metricNames,
    onAction: onAction,
    add: add, addAll: addAll, get: get, update: update, remove: remove,
    toggle: toggle, list: list, clear: clear,
    evalRule: evalRule, tick: tick, evaluateNow: evaluateNow, reset: reset,
    snapshot: snapshot, restore: restore,
    ops: function () { return Object.keys(OPS); }
  };
  return engine;
}

window.weld.rules = {
  create: createEngine,
  version: '1.0.0'
};

return window.weld.rules;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
