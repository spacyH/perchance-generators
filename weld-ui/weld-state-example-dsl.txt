$meta
  title       = weld.state examples
  description = Interactive demo of weld.state -- typed story state with an AI audit pass. Edit fields, see the prompt block, and reconcile state from narrative.
  tags        = weld, state, tracking, example, plugin

  weldStructured = {import:weld-structured-plugin}
  weldStream     = {import:weld-stream-plugin}
  weldState      = {import:weld-state-plugin}
  aiTextPlugin   = {import:ai-text-plugin}
