$meta
  title       = weld.structured examples
  description = Interactive demo of weld.structured. Get reliable JSON out of a chatty model: parse the object out of prose and code fences, then validate it against a schema.
  tags        = weld, structured, example, demo, plugin

  weldStructured   = {import:weld-structured-plugin}
