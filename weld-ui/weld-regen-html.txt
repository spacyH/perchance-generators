<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.changelog{margin:14px 0;padding:14px 16px;background:var(--faint);border:1px solid var(--border);border-radius:6px;}
.changelog .cl-ver{font:700 13px var(--mono);color:var(--accent);margin:10px 0 4px;}
.changelog .cl-ver:first-child{margin-top:0;}
.changelog ul{margin:0 0 4px;padding-left:18px;}
.changelog li{font-size:13px;color:#c9d1d9;line-height:1.5;margin:2px 0;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.regen<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-regen-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.regen</h1>
      <span class="ver">v1.0.1</span>
  </div>
  <p class="lead">Regenerate and swipe between alternate replies. The bridge between weld.stream and weld.branching: each new take becomes a sibling node, so you keep every variant.</p>
</div>
<h2>Install</h2><pre data-codeid="install"></pre>
<h2>Quick start</h2><pre data-codeid="quick"></pre>
<h2>regenerate(opts)</h2><div class="sig" data-codeid="sig_regen"></div>
<p>Re-runs generation and appends the result as a sibling of <code>opts.nodeId</code> (default the active leaf). Pass <code>build()</code> or <code>instruction</code> for the prompt, plus any weld.stream run options.</p>
<h2>swipe / variants / edit</h2>
<div class="sig" data-codeid="sig_swipe"></div><div class="sig" data-codeid="sig_variants"></div><div class="sig" data-codeid="sig_edit"></div>
<pre data-codeid="swipe_ex"></pre>
<h2>Attaching dependencies <span class="tag">v1.0.1</span></h2>
<div class="sig" data-codeid="sig_attach"></div>
<p><code>attach(deps)</code> lets an app inject the branching tree explicitly instead of relying on <code>window.weld.branching</code> discovery &mdash; useful when wiring weld.regen against a specific session&rsquo;s tree.</p>
<pre data-codeid="attach_ex"></pre>
<h2>Changelog</h2>
<div class="changelog">
<p class="cl-ver">v1.0.1</p>
<ul>
<li>Fixed swipe(): switchSibling now normalizes a returned id-string OR node object, so swiping between takes is reliable. Exposed attach() for explicit dependency wiring.</li>
</ul>
<p class="cl-ver">v1.0.0</p>
<ul>
<li>Initial release: regenerate/swipe over a branching tree.</li>
</ul>
</div>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-regen     = \{import:weld-regen-plugin\}\n  weld-stream    = \{import:weld-stream-plugin\}\n  weld-branching = \{import:weld-branching-plugin\}\n  aiTextPlugin   = \{import:ai-text-plugin\}',
  'quick': 'var tree = weld.branching.create();\n// ... build the conversation ...\nvar res = await weld.regen.regenerate(\{ tree: tree, instruction: prompt \});\nrender(res.node);  // a new sibling take, now active',
  'sig_regen': 'weld.regen.regenerate(opts) -> Promise<\{ node, result, error \}>',
  'sig_swipe': 'weld.regen.swipe(tree, nodeId, dir) -> node',
  'sig_variants': 'weld.regen.variants(tree, nodeId) -> \{ index, count, items \}',
  'sig_edit': 'weld.regen.edit(tree, nodeId, newContent) -> node',
  'swipe_ex': 'var v = weld.regen.variants(tree, id);   // \{ index: 2, count: 3, items: \[...] \}\nweld.regen.swipe(tree, id, +1);          // next take\nweld.regen.swipe(tree, id, -1);          // previous take',
  'sig_attach': 'regen.attach(\{ branching \})',
  'attach_ex': 'weld.regen.attach(\{ branching: mySession.tree \});',
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.regen) {
  window.weld.regen.attach({ stream: window.weld.stream, branching: window.weld.branching });
  return window.weld.regen;
}

// -----------------------------------------------------------------
// weld.regen: the "swipe" feature every chat app wants, built as the
// connective tissue between two plugins that already exist.
//
//   weld.branching  gives you the conversation tree (nodes, siblings).
//   weld.stream     runs the model.
//
// weld.regen ties them together: re-running the current assistant reply
// appends the new take as a SIBLING of the old one (a branch from the
// same parent), so you keep every alternative and can swipe between
// them -- exactly the character.ai style swipe deck.
//
// You supply how to build the instruction (a build() callback or a
// literal instruction); weld.regen owns the tree bookkeeping.
// -----------------------------------------------------------------

var deps = { stream: null, branching: null };
function attach(d) {
  if (d && d.stream) deps.stream = d.stream;
  if (d && d.branching) deps.branching = d.branching;
}
function stream() { return deps.stream || (window.weld && window.weld.stream) || null; }
function branching() { return deps.branching || (window.weld && window.weld.branching) || null; }

function resolveInstruction(opts) {
  if (typeof opts.build === 'function') return Promise.resolve(opts.build());
  if (opts.instruction != null) return Promise.resolve(opts.instruction);
  return Promise.resolve(null);
}

// Run one generation through weld.stream, forwarding stream callbacks.
function generate(instruction, opts) {
  var s = stream();
  if (!s) return Promise.resolve({ text: '', error: 'weld.regen: weld.stream is not loaded' });
  var runOpts = { instruction: instruction };
  ['startWith', 'hideStartWith', 'stopSequences', 'chat', 'onChunk', 'onToken'].forEach(function (k) {
    if (opts[k] != null) runOpts[k] = opts[k];
  });
  return s.run(runOpts);
}

// -----------------------------------------------------------------
// regenerate(opts) -> Promise<{ node, result, error }>
//
// opts:
//   tree         (required) a weld.branching tree
//   nodeId       the assistant node to make a sibling of
//                (default: the active leaf, tree.getActiveId())
//   build()/instruction   how to produce the prompt (see above)
//   data(result) optional: map a stream result -> node data
//                (default: { role:'assistant', content: result.text, meta })
//   setActive    (bool) switch the tree to the new node (default true)
//   ...plus any weld.stream run options (onChunk, chat, startWith, ...)
// -----------------------------------------------------------------
function regenerate(opts) {
  opts = opts || {};
  var tree = opts.tree;
  if (!tree || typeof tree.append !== 'function') {
    return Promise.resolve({ node: null, result: null, error: 'weld.regen: opts.tree must be a weld.branching tree' });
  }
  var targetId = opts.nodeId || tree.getActiveId();

  return resolveInstruction(opts).then(function (instruction) {
    if (instruction == null || String(instruction).trim() === '') {
      return { node: null, result: null, error: 'weld.regen: no instruction (pass build() or instruction)' };
    }
    return generate(instruction, opts).then(function (result) {
      if (result.error) return { node: null, result: result, error: result.error };

      var data = (typeof opts.data === 'function')
        ? opts.data(result)
        : { role: 'assistant', content: result.text, meta: { stopReason: result.stopReason, truncated: result.truncated, regenerated: true } };

      var node;
      var existing = targetId ? tree.get(targetId) : null;
      if (existing && existing.parentId) {
        // Sibling: branch from the same parent as the target node.
        node = tree.branch(targetId, data);
      } else if (existing) {
        // Target is a root: add another root-level sibling via append(null parent).
        node = tree.append(data, null);
      } else {
        // No target yet: just append at the active position.
        node = tree.append(data);
      }
      if (opts.setActive !== false && node && node.id) tree.setActive(node.id);
      return { node: node, result: result, error: null };
    });
  });
}

// Swipe among the siblings of a node. dir: +1 / -1 (default +1).
// Returns the node now active, or null.
function swipe(tree, nodeId, dir) {
  if (!tree) return null;
  var id = nodeId || tree.getActiveId();
  if (!id) return null;
  if (typeof tree.switchSibling === 'function') {
    var moved = tree.switchSibling(id, (dir == null ? 1 : dir));
    if (moved == null) return null;
    // switchSibling may return either a node object or a bare id string,
    // depending on the tree implementation. Normalize to an id, switch the
    // active leaf, and hand back the node.
    var movedId = (typeof moved === 'object') ? moved.id : moved;
    if (movedId && tree.setActive) tree.setActive(movedId);
    return (typeof moved === 'object') ? moved : (tree.get ? tree.get(movedId) : { id: movedId });
  }
  return null;
}

// The list of sibling takes for a node, with a 1-based position.
// Returns { index, count, items:[{ id, data, active }] }.
function variants(tree, nodeId) {
  var empty = { index: 0, count: 0, items: [] };
  if (!tree || typeof tree.siblings !== 'function') return empty;
  var id = nodeId || tree.getActiveId();
  if (!id) return empty;
  var sibs = tree.siblings(id) || [];
  var activeId = tree.getActiveId ? tree.getActiveId() : id;
  var items = sibs.map(function (n) {
    return { id: n.id, data: n.data, active: n.id === activeId };
  });
  var index = 0;
  for (var i = 0; i < items.length; i++) if (items[i].active) { index = i + 1; break; }
  return { index: index, count: items.length, items: items };
}

// Edit a node's text in place (e.g. the user fixes the reply).
function edit(tree, nodeId, newContent) {
  if (!tree) return null;
  var n = tree.get(nodeId);
  if (!n) return null;
  if (n.data && typeof n.data === 'object') n.data.content = newContent;
  else n.data = { content: newContent };
  return n;
}

window.weld.regen = {
  attach: attach,
  regenerate: regenerate,
  swipe: swipe,
  variants: variants,
  edit: edit,
  version: '1.0.1'
};

window.weld.regen.attach({ stream: window.weld.stream, branching: window.weld.branching });
return window.weld.regen;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
