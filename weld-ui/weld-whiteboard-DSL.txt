title
  weld-whiteboard
description
  A live, multi-user collaborative whiteboard. Draw together and see each other's cursors in real time.
tags
  weld, weld-companion, weld-ui, ai, skybridge, live, whiteboard, paint, collab, group, drawing, app
// ── plugins ──────────────────────────────────────────────────────────────────
// super-fetch-plugin exposes root.superFetch(url, init) to the HTML panel: a
// server-side CORS proxy so the sandboxed page can reach the sync relay over HTTP
// (the sandbox can't open arbitrary cross-origin connections itself). This single
// import is what makes the multiplayer transport in the HTML panel work.
superFetch = {import:super-fetch-plugin}

// The entire app lives in the HTML panel; it reaches the plugin via root.superFetch.
// This output is intentionally minimal and is not referenced by the HTML, so it
// never renders — it just keeps the generator well-formed.
output
  weld-whiteboard runs in the HTML panel.