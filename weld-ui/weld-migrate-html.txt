<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.migrate<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-migrate-example">demo &rarr;</a></span></div>  <div class="hdr">
    <div class="hdr-row">
      <h1>weld.migrate</h1>
      <span class="ver">v1.0.0</span>
    </div>
    <p class="lead">Versioned IndexedDB schema migrations with a Promise-based wrapper. Declarative schema, automatic data migrations, transaction support.</p>
  </div>
  <h2>Overview</h2>
<p>weld.migrate is a small, declarative wrapper around IndexedDB. You declare your schema by version number; the plugin handles opening the database, applying schema upgrades, running data backfills, and exposing a friendly Promise-based API.</p>
<p>Built to solve the problem nobody thinks about until v2: <strong>your existing users have data in the v1 shape</strong>. With weld.migrate, you write a migration for each version and never have to special-case old data again.</p>
<h2>Quick start</h2>
<p>Add to the lists editor of your generator:</p>
<pre data-codeid="code1"></pre>

<p>In your HTML panel, declare the schema and open the database:</p>
<pre data-codeid="code2"></pre>

<p>Bump to v2 by adding a new entry to the schema. The plugin detects the version delta and applies just the new step:</p>
<pre data-codeid="code3"></pre>
<h2>Schema declaration</h2>
<p>The <code>schema</code> object is keyed by version number. Each entry can have three optional fields:</p>

<h4>stores</h4>
<p>Create new object stores. Each store can specify <code>keyPath</code>, <code>autoIncrement</code>, and <code>indexes</code>.</p>
<pre data-codeid="code4"></pre>

<h4>modify</h4>
<p>Modify an existing store. Currently supports adding and dropping indexes.</p>
<pre data-codeid="code5"></pre>

<h4>drop</h4>
<p>Remove stores entirely. Their data is irrecoverable after this runs.</p>
<pre data-codeid="code6"></pre>
<h2>Data migrations</h2>
<p>Data migrations are async functions that run <em>after</em> the schema upgrade. They're tracked separately in a meta store, so each runs exactly once per browser, even if you add them later.</p>

<pre data-codeid="code7"></pre>

<div class="note">Data migrations receive the wrapped db (the same object that <code>open()</code> resolves with), so all the friendly methods are available. Each migration runs in its own transactions, so you can <code>await</code> freely.</div>
<h2>DB methods</h2>
<p>The object returned by <code>open()</code> wraps the raw IDBDatabase with Promise-based methods. All methods that read or write data return a Promise resolving to the result.</p>
<h3>Reading</h3>
<div class="sig" data-codeid="sig8"></div>
<p>Get a single record by primary key.</p>

<div class="sig" data-codeid="sig9"></div>
<p>Get all records from a store. <code>opts</code>: <code>{ range, limit }</code>.</p>

<div class="sig" data-codeid="sig10"></div>
<p>Look up all records by an index match.</p>

<div class="sig" data-codeid="sig11"></div>
<p>Like <code>query</code> but returns just the first match.</p>

<div class="sig" data-codeid="sig12"></div>
<p>Index lookup with a range.</p>

<pre data-codeid="code13"></pre>

<div class="sig" data-codeid="sig14"></div>
<div class="sig" data-codeid="sig15"></div>
<p>Count records or list keys (without loading values).</p>

<div class="sig" data-codeid="sig16"></div>
<p>Iterate every record without loading them all into memory. Useful for huge stores. Return <code>false</code> from the callback to stop iteration.</p>

<pre data-codeid="code17"></pre>
<h3>Writing</h3>
<div class="sig" data-codeid="sig18"></div>
<p>Insert or update a record. Returns the key.</p>

<div class="sig" data-codeid="sig19"></div>
<div class="sig" data-codeid="sig20"></div>
<p>Delete by key, or clear all records in a store.</p>

<div class="sig" data-codeid="sig21"></div>
<div class="sig" data-codeid="sig22"></div>
<p>Put or delete many items in a single transaction. Faster than calling <code>put</code> in a loop.</p>

<pre data-codeid="code23"></pre>
<h3>Transactions</h3>
<div class="sig" data-codeid="sig24"></div>
<p>Run multiple operations atomically. The callback receives a helper object with <code>put</code>, <code>get</code>, <code>delete</code>, <code>getAll</code>, <code>clear</code>, <code>count</code> methods scoped to the open transaction.</p>

<pre data-codeid="code25"></pre>
<h2>Utilities</h2>
<div class="sig" data-codeid="sig26"></div>
<p>Delete an entire database. Cannot be undone.</p>

<div class="sig" data-codeid="sig27"></div>
<p>List all databases the browser knows about (Chrome / Firefox 126+).</p>

<div class="sig" data-codeid="sig28"></div>
<p>Returns the current schema version of a database, or 0 if it doesn't exist. Useful for diagnostics.</p>
<h2>Notes</h2>
<div class="note">Schema upgrades happen inside IndexedDB's special <em>upgradeneeded</em> transaction, which is sync-only. The plugin handles this for you - your migration steps describe what to do declaratively, and the plugin executes them in the right transaction.</div>
<div class="warn">Data migrations run AFTER the schema is up to date, so they can use the friendly Promise-based API. They're tracked per-browser in a hidden <code>_weldMeta</code> store - if a user clears their data, all migrations re-run on the fresh schema.</div>
<div class="note">If the same database is open in another tab with an older version, the open call will block until the other tab closes the connection. The plugin emits an <code>onProgress</code> message in that case.</div></div>
<script>
(function(){
  var examples = {
  'code1': 'output\n  weld-migrate = \{import:weld-migrate-plugin\}',
  'code2': 'var db = await weld.migrate.open(\'mychat\', \{\n  schema: \{\n    1: \{\n      stores: \{\n        characters: \{ keyPath: \'id\', indexes: \{ byName: \'name\' \} \},\n        messages:   \{ keyPath: \'id\', autoIncrement: true \},\n      \}\n    \},\n  \}\n\});\n\nawait db.put(\'characters\', \{ id: \'1\', name: \'Aria\' \});\nvar aria = await db.get(\'characters\', \'1\');\nvar all  = await db.getAll(\'characters\');',
  'code3': 'schema: \{\n  1: \{ stores: \{ characters: \{ keyPath: \'id\' \} \} \},\n  2: \{\n    modify: \{ characters: \{ addIndexes: \{ byTag: \'tag\' \} \} \},\n    stores: \{ threads: \{ keyPath: \'id\' \} \},\n  \}\n\}',
  'code4': 'stores: \{\n  characters: \{\n    keyPath: \'id\',\n    indexes: \{\n      byName: \'name\',                              // shorthand: just the keyPath\n      byCreated: \{ keyPath: \'created\', unique: false \},\n      byTags: \{ keyPath: \'tags\', multiEntry: true \},  // array indexing\n    \}\n  \},\n  events: \{ keyPath: \'id\', autoIncrement: true \},  // no indexes\n\}',
  'code5': 'modify: \{\n  characters: \{\n    addIndexes: \{ byArchived: \'archived\' \},\n    dropIndexes: \[\'byCreated\'\],\n  \}\n\}',
  'code6': 'drop: \[\'oldStuff\', \'tempCache\'\]',
  'code7': 'var db = await weld.migrate.open(\'mychat\', \{\n  schema: \{ /* ... */ \},\n  dataMigrations: \{\n    2: async (db) => \{\n      // Backfill a new field on existing records\n      var chars = await db.getAll(\'characters\');\n      for (var c of chars) \{\n        c.tag = c.tag || \'untagged\';\n        await db.put(\'characters\', c);\n      \}\n    \},\n    3: async (db) => \{\n      // Split a field into two\n      var chars = await db.getAll(\'characters\');\n      for (var c of chars) \{\n        var parts = (c.name || \'\').split(\' \');\n        c.firstName = parts\[0\];\n        c.lastName  = parts.slice(1).join(\' \');\n        delete c.name;\n        await db.put(\'characters\', c);\n      \}\n    \},\n  \}\n\});',
  'sig8': 'db.get(store, key)',
  'sig9': 'db.getAll(store, opts?)',
  'sig10': 'db.query(store, indexName, value)',
  'sig11': 'db.queryFirst(store, indexName, value)',
  'sig12': 'db.queryRange(store, indexName, IDBKeyRange)',
  'code13': 'var aria = await db.queryFirst(\'characters\', \'byName\', \'Aria\');\nvar recent = await db.queryRange(\'messages\', \'byTime\',\n  IDBKeyRange.lowerBound(Date.now() - 86400000));',
  'sig14': 'db.count(store)',
  'sig15': 'db.keys(store)',
  'sig16': 'db.each(store, callback)',
  'code17': 'await db.each(\'messages\', (msg, key) => \{\n  if (msg.timestamp < cutoff) \{\n    db.delete(\'messages\', key);\n  \}\n\});',
  'sig18': 'db.put(store, value)',
  'sig19': 'db.delete(store, key)',
  'sig20': 'db.clear(store)',
  'sig21': 'db.bulkPut(store, items)',
  'sig22': 'db.bulkDelete(store, keys)',
  'code23': 'await db.bulkPut(\'characters\', \[\n  \{ id: \'1\', name: \'Aria\' \},\n  \{ id: \'2\', name: \'Bex\' \},\n  \{ id: \'3\', name: \'Cy\' \},\n\]);',
  'sig24': 'db.tx(stores, mode, callback)',
  'code25': 'await db.tx(\[\'characters\', \'threads\'\], \'readwrite\', async (tx) => \{\n  await tx.put(\'characters\', char);\n  await tx.put(\'threads\', thread);\n  // If anything throws here, all changes roll back.\n\});',
  'sig26': 'weld.migrate.deleteDatabase(name)',
  'sig27': 'weld.migrate.listDatabases()',
  'sig28': 'weld.migrate.currentVersion(name)'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    var nodes = document.querySelectorAll('[data-codeid]');
    nodes.forEach(function(n){
      var id = n.getAttribute('data-codeid');
      var src = examples[id];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.migrate) return;

// META store name -- tracks which data migrations have been applied
var META_STORE = '_weldMeta';
var META_KEY = 'appliedDataMigrations';

// Wrap an IDBRequest in a Promise
function req(idbRequest) {
  return new Promise(function(resolve, reject) {
    idbRequest.onsuccess = function(){ resolve(idbRequest.result); };
    idbRequest.onerror = function(){ reject(idbRequest.error); };
  });
}

// Wait for transaction completion
function txComplete(tx) {
  return new Promise(function(resolve, reject) {
    tx.oncomplete = function(){ resolve(); };
    tx.onerror = function(){ reject(tx.error); };
    tx.onabort = function(){ reject(tx.error || new Error('Transaction aborted')); };
  });
}

// Normalize an index spec - accepts string keyPath OR full object
function normalizeIndex(spec) {
  if (typeof spec === 'string') return { keyPath: spec, unique: false };
  return Object.assign({ unique: false }, spec);
}

// Apply schema upgrade INSIDE upgradeneeded handler
function applySchema(db, tx, schema, fromVer, toVer, log) {
  // Always ensure meta store exists from v1
  if (!db.objectStoreNames.contains(META_STORE)) {
    db.createObjectStore(META_STORE, { keyPath: 'key' });
    log('created meta store ' + META_STORE);
  }

  // Apply each version step
  for (var v = fromVer + 1; v <= toVer; v++) {
    var step = schema[v];
    if (!step) continue;
    log('applying schema v' + v);

    // Create new stores
    if (step.stores) {
      Object.keys(step.stores).forEach(function(name){
        if (db.objectStoreNames.contains(name)) {
          log('  store ' + name + ' already exists, skipping create');
          return;
        }
        var sp = step.stores[name];
        var opts = {};
        if (sp.keyPath) opts.keyPath = sp.keyPath;
        if (sp.autoIncrement) opts.autoIncrement = true;
        var store = db.createObjectStore(name, opts);
        log('  + store ' + name);
        if (sp.indexes) {
          Object.keys(sp.indexes).forEach(function(idxName){
            var idx = normalizeIndex(sp.indexes[idxName]);
            store.createIndex(idxName, idx.keyPath, { unique: idx.unique, multiEntry: !!idx.multiEntry });
            log('    + index ' + idxName);
          });
        }
      });
    }

    // Modify existing stores
    if (step.modify) {
      Object.keys(step.modify).forEach(function(name){
        if (!db.objectStoreNames.contains(name)) {
          log('  cannot modify ' + name + ' (does not exist)');
          return;
        }
        var store = tx.objectStore(name);
        var mods = step.modify[name];
        if (mods.dropIndexes) {
          mods.dropIndexes.forEach(function(idxName){
            if (store.indexNames.contains(idxName)) {
              store.deleteIndex(idxName);
              log('  - index ' + name + '.' + idxName);
            }
          });
        }
        if (mods.addIndexes) {
          Object.keys(mods.addIndexes).forEach(function(idxName){
            if (store.indexNames.contains(idxName)) return;
            var idx = normalizeIndex(mods.addIndexes[idxName]);
            store.createIndex(idxName, idx.keyPath, { unique: idx.unique, multiEntry: !!idx.multiEntry });
            log('  + index ' + name + '.' + idxName);
          });
        }
      });
    }

    // Drop stores
    if (step.drop) {
      step.drop.forEach(function(name){
        if (db.objectStoreNames.contains(name)) {
          db.deleteObjectStore(name);
          log('  - store ' + name);
        }
      });
    }
  }
}

// Open a database with declarative schema + data migrations
function open(name, config) {
  config = config || {};
  var schema = config.schema || {};
  var dataMigrations = config.dataMigrations || {};
  var onProgress = config.onProgress || function(){};

  // Highest schema version
  var versions = Object.keys(schema).map(Number).filter(function(n){ return !isNaN(n); });
  var targetVersion = versions.length ? Math.max.apply(null, versions) : 1;

  return new Promise(function(resolve, reject) {
    var req = indexedDB.open(name, targetVersion);

    req.onerror = function(){
      onProgress('error: ' + (req.error && req.error.message));
      reject(req.error);
    };

    req.onupgradeneeded = function(e){
      var db = req.result;
      var tx = e.target.transaction;
      var oldVer = e.oldVersion;
      var newVer = e.newVersion;
      onProgress('upgrading ' + name + ' from v' + oldVer + ' to v' + newVer);
      try {
        applySchema(db, tx, schema, oldVer, newVer, onProgress);
      } catch (err) {
        onProgress('schema error: ' + err.message);
        tx.abort();
        reject(err);
      }
    };

    req.onblocked = function(){
      onProgress('blocked: another connection has an older version open');
    };

    req.onsuccess = function(){
      var db = req.result;
      db.onversionchange = function(){
        db.close();
        onProgress('closed due to newer version opening');
      };

      // Run data migrations
      runDataMigrations(db, dataMigrations, onProgress).then(function(){
        resolve(wrapDb(db, name));
      }).catch(reject);
    };
  });
}

// Run any data migrations whose version hasn't been applied yet
function runDataMigrations(db, migrations, onProgress) {
  var versions = Object.keys(migrations).map(Number).sort(function(a, b){ return a - b; });
  if (!versions.length) return Promise.resolve();

  // Read applied set from meta
  var tx = db.transaction([META_STORE], 'readonly');
  var store = tx.objectStore(META_STORE);
  return req(store.get(META_KEY)).then(function(entry){
    var applied = (entry && entry.value) || [];
    var todo = versions.filter(function(v){ return applied.indexOf(v) === -1; });
    if (!todo.length) return;

    onProgress('running ' + todo.length + ' data migration(s): ' + todo.join(', '));
    var wrapped = wrapDb(db, '_internal');
    var chain = Promise.resolve();
    todo.forEach(function(v){
      chain = chain.then(function(){
        onProgress('  data migration v' + v + ' starting');
        return Promise.resolve(migrations[v](wrapped));
      }).then(function(){
        onProgress('  data migration v' + v + ' done');
        // Mark applied
        var tx2 = db.transaction([META_STORE], 'readwrite');
        var s2 = tx2.objectStore(META_STORE);
        return req(s2.get(META_KEY)).then(function(cur){
          var arr = (cur && cur.value) || [];
          if (arr.indexOf(v) === -1) arr.push(v);
          return req(s2.put({ key: META_KEY, value: arr }));
        }).then(function(){ return txComplete(tx2); });
      });
    });
    return chain;
  });
}

// Wrap a raw IDBDatabase in a friendly Promise-based API
function wrapDb(db, name) {
  function tx(stores, mode, callback) {
    if (!Array.isArray(stores)) stores = [stores];
    var t = db.transaction(stores, mode || 'readonly');
    var helper = {
      _tx: t,
      objectStore: function(s){ return t.objectStore(s); },
      put: function(s, v){ return req(t.objectStore(s).put(v)); },
      get: function(s, k){ return req(t.objectStore(s).get(k)); },
      delete: function(s, k){ return req(t.objectStore(s).delete(k)); },
      getAll: function(s){ return req(t.objectStore(s).getAll()); },
      clear: function(s){ return req(t.objectStore(s).clear()); },
      count: function(s){ return req(t.objectStore(s).count()); },
    };
    var result = Promise.resolve(callback(helper));
    return result.then(function(r){
      return txComplete(t).then(function(){ return r; });
    });
  }

  return {
    name: name,
    _db: db,
    version: db.version,
    storeNames: Array.from(db.objectStoreNames),

    put: function(store, value) {
      var t = db.transaction([store], 'readwrite');
      return req(t.objectStore(store).put(value));
    },
    get: function(store, key) {
      var t = db.transaction([store], 'readonly');
      return req(t.objectStore(store).get(key));
    },
    delete: function(store, key) {
      var t = db.transaction([store], 'readwrite');
      return req(t.objectStore(store).delete(key));
    },
    getAll: function(store, opts) {
      opts = opts || {};
      var t = db.transaction([store], 'readonly');
      var s = t.objectStore(store);
      if (opts.limit != null) return req(s.getAll(opts.range || null, opts.limit));
      return req(s.getAll(opts.range || null));
    },
    count: function(store) {
      var t = db.transaction([store], 'readonly');
      return req(t.objectStore(store).count());
    },
    clear: function(store) {
      var t = db.transaction([store], 'readwrite');
      return req(t.objectStore(store).clear());
    },
    query: function(store, indexName, value) {
      var t = db.transaction([store], 'readonly');
      var s = t.objectStore(store);
      var idx = s.index(indexName);
      return req(idx.getAll(value));
    },
    queryFirst: function(store, indexName, value) {
      var t = db.transaction([store], 'readonly');
      var s = t.objectStore(store);
      var idx = s.index(indexName);
      return req(idx.get(value));
    },
    queryRange: function(store, indexName, range) {
      var t = db.transaction([store], 'readonly');
      var s = t.objectStore(store);
      var idx = s.index(indexName);
      return req(idx.getAll(range));
    },
    keys: function(store) {
      var t = db.transaction([store], 'readonly');
      return req(t.objectStore(store).getAllKeys());
    },
    tx: tx,
    close: function() { db.close(); },

    // Bulk helpers
    bulkPut: function(store, items) {
      var t = db.transaction([store], 'readwrite');
      var s = t.objectStore(store);
      var promises = items.map(function(item){ return req(s.put(item)); });
      return Promise.all(promises).then(function(){ return txComplete(t); });
    },
    bulkDelete: function(store, keys) {
      var t = db.transaction([store], 'readwrite');
      var s = t.objectStore(store);
      var promises = keys.map(function(k){ return req(s.delete(k)); });
      return Promise.all(promises).then(function(){ return txComplete(t); });
    },

    // Iterator for huge datasets - avoids loading all into memory
    each: function(store, callback) {
      var t = db.transaction([store], 'readonly');
      var s = t.objectStore(store);
      return new Promise(function(resolve, reject) {
        var cur = s.openCursor();
        cur.onsuccess = function(){
          var c = cur.result;
          if (!c) { resolve(); return; }
          try {
            var r = callback(c.value, c.key);
            if (r === false) { resolve(); return; }
          } catch (e) { reject(e); return; }
          c.continue();
        };
        cur.onerror = function(){ reject(cur.error); };
      });
    },
  };
}

// Delete a whole database
function deleteDatabase(name) {
  return new Promise(function(resolve, reject) {
    var req = indexedDB.deleteDatabase(name);
    req.onsuccess = function(){ resolve(); };
    req.onerror = function(){ reject(req.error); };
    req.onblocked = function(){ /* still waiting */ };
  });
}

// List all databases (Chrome/Firefox 126+, may not exist in older browsers)
function listDatabases() {
  if (indexedDB.databases) {
    return indexedDB.databases();
  }
  return Promise.resolve([]);
}

// Get current version of a DB without opening it for writes
function currentVersion(name) {
  return new Promise(function(resolve) {
    var req = indexedDB.open(name);
    req.onsuccess = function(){
      var v = req.result.version;
      req.result.close();
      resolve(v);
    };
    req.onerror = function(){ resolve(0); };
  });
}

// PUBLIC API
window.weld.migrate = {
  open: open,
  deleteDatabase: deleteDatabase,
  listDatabases: listDatabases,
  currentVersion: currentVersion,
  version: '1.0.0',
};

})();

</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
