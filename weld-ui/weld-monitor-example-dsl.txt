$meta
title = weld.monitor live suite
description = The live Perchance Platform Monitor -- run ~100 assertions across 8 groups and get a pass/fail/drift/skip report with a stable fingerprint. Imports the weld.monitor plugin plus the core platform plugins it probes.
tags = weld, monitor, test, platform, example, plugin

weldMonitor = {import:weld-monitor-plugin}
aiTextPlugin = {import:ai-text-plugin}
textToImagePlugin = {import:text-to-image-plugin}
uploadPlugin = {import:upload-plugin}
superFetch = {import:super-fetch-plugin}
kv = {import:kv-plugin}

monitorTestList
alpha
bravo
charlie

monitorTestFunc(x) => "echo:" + x