$meta
title = weld.upload examples
description = Interactive demo of weld.upload -- a v2 of upload-plugin with pre-upload validation, a normalized result, progress, and batch. Pick a file and watch validation + upload.
tags = weld, upload, files, v2, example, plugin

weldUpload = {import:weld-upload-plugin}
uploadPlugin = {import:upload-plugin}