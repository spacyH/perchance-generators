$meta
  title       = weld.migrate — Examples
  description = Live demo of weld.migrate as a tiny contact book. Add, edit, delete, search, and watch the data persist across reloads. Demonstrates schema definition, indexes, and the wrapped DB API.
  tags        = weld, indexeddb, migrate, example, plugin

weldUi      = {import:weld-ui-plugin}
weldMigrate = {import:weld-migrate-plugin}
