$meta
  title       = weld.comments examples
  description = Interactive demo of weld.comments. Drop a discussion thread onto anything, or use the same comments-plugin as a serverless append store.
  tags        = weld, comments, example, demo, plugin

  weldComments     = {import:weld-comments-plugin}
