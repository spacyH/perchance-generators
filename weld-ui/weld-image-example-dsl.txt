$meta
  title       = weld.image examples
  description = Style preset picker, resolution presets, single and batch generation with progress. Uses text-to-image-plugin when present.
  tags        = weld, image, t2i, generation, example, plugin

weldUi    = {import:weld-ui-plugin}
weldImage = {import:weld-image-plugin}
