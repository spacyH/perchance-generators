$meta
  title       = weld.clean examples
  description = Interactive demo of weld.clean. Scrub raw model text before you store or render it: trim a dangling sentence after truncation, strip role-name bleed, collapse repetition, and balance code fences.
  tags        = weld, clean, example, demo, plugin

  weldClean        = {import:weld-clean-plugin}
