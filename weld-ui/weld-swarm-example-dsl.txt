$meta
  title = weld.swarm examples
  description = Live demo of weld.swarm -- generator-side multi-agent orchestration. A driver hands a batch of subtasks to worker agents, each running weld.crew, and collects the results in parallel. Run a self-contained local demo in one tab, or set this tab role to Worker or User and open it in two tabs to coordinate over weld.sync on the same origin. Imports weld.skybridge, weld.crew, weld.swarm, and weld.sync.
  tags = weld, swarm, orchestrator, agent, multiplayer, example, plugin

weldSkybridge = {import:weld-skybridge-plugin}
weldSync      = {import:weld-sync-plugin}
weldCrew      = {import:weld-crew-plugin}
weldSwarm     = {import:weld-swarm-plugin}
