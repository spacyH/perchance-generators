$meta
  title       = weld.tokens examples
  description = Live tokenizer demo: real-time exact counts, budget visualization, truncation preview, document chunking.
  tags        = weld, tokens, tokenizer, example, plugin

weldUi     = {import:weld-ui-plugin}
weldTokens = {import:weld-tokens-plugin}
