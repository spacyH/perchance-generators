$meta
  title       = weld.persist examples
  description = Live IndexedDB CRUD demo: add, edit, delete records in switchable collections. Export and import the full database.
  tags        = weld, persist, database, indexeddb, example, plugin

weldUi      = {import:weld-ui-plugin}
weldPersist = {import:weld-persist-plugin}
