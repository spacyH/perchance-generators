<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.konami<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-konami-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.konami</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">A key-sequence detector for Easter eggs. Watch for the Konami code, a custom sequence, or a typed word, and fire your callback -- by default a confetti burst if weld.confetti is loaded.</p>
</div>
<h2>Install</h2><pre data-codeid="install"></pre>
<h2>Watch a sequence</h2>
<div class="sig" data-codeid="sig_konami"></div><div class="sig" data-codeid="sig_seq"></div><div class="sig" data-codeid="sig_code"></div>
<pre data-codeid="konami_ex"></pre>
<h2>Testing</h2><div class="sig" data-codeid="sig_feed"></div>
<p>Each register call returns an unsubscribe function. <code>feed(key)</code> simulates a keypress for tests.</p>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-konami   = \{import:weld-konami-plugin\}\n  weld-confetti = \{import:weld-confetti-plugin\}',
  'sig_konami': 'weld.konami.onKonami(cb) -> unsubscribe',
  'sig_seq': 'weld.konami.onSequence(keys, cb) -> unsubscribe',
  'sig_code': 'weld.konami.onCode(word, cb) -> unsubscribe',
  'konami_ex': 'weld.konami.onKonami();                       // default: fireworks\nweld.konami.onCode(\'xyzzy\', unlockSecret);   // a typed word',
  'sig_feed': 'weld.konami.feed(key) / reset() / KONAMI'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.konami) return window.weld.konami;

// -----------------------------------------------------------------
// weld.konami: the secret handshake. Register a sequence of keys (or a
// typed word) and a callback; weld.konami keeps a rolling buffer of
// recent keydowns and fires when the tail matches. The classic code is
// built in. With no callback it pops confetti, if weld.confetti is around.
// -----------------------------------------------------------------

// Up Up Down Down Left Right Left Right B A
var KONAMI = ['ArrowUp', 'ArrowUp', 'ArrowDown', 'ArrowDown',
  'ArrowLeft', 'ArrowRight', 'ArrowLeft', 'ArrowRight', 'b', 'a'];

var watchers = [];   // { seq:[normalized keys], cb, buffer:[] }
var attached = false;

function normalizeKey(k) {
  if (k == null) return '';
  if (k.length === 1) return k.toLowerCase();
  return k; // named keys like ArrowUp keep their case
}

function tailMatches(buffer, seq) {
  if (buffer.length < seq.length) return false;
  var offset = buffer.length - seq.length;
  for (var i = 0; i < seq.length; i++) {
    if (buffer[offset + i] !== seq[i]) return false;
  }
  return true;
}

function onKeydown(e) {
  var key = normalizeKey(e.key);
  if (!key) return;
  for (var i = 0; i < watchers.length; i++) {
    var w = watchers[i];
    w.buffer.push(key);
    if (w.buffer.length > w.seq.length) w.buffer.shift();
    if (tailMatches(w.buffer, w.seq)) {
      w.buffer = [];
      try { w.cb(); } catch (err) { /* swallow */ }
    }
  }
}

function ensureAttached() {
  if (attached || typeof document === 'undefined') return;
  document.addEventListener('keydown', onKeydown);
  attached = true;
}

function defaultPayload() {
  if (window.weld && window.weld.confetti) window.weld.confetti.fireworks({ shots: 4 });
  else if (window.weld && window.weld.toast) window.weld.toast.success('Achievement unlocked!');
}

function register(seq, cb) {
  var normalized = (seq || []).map(normalizeKey);
  if (!normalized.length) return function () {};
  var watcher = { seq: normalized, cb: cb || defaultPayload, buffer: [] };
  watchers.push(watcher);
  ensureAttached();
  return function unsubscribe() {
    var idx = watchers.indexOf(watcher);
    if (idx !== -1) watchers.splice(idx, 1);
  };
}

// The classic code.
function onKonami(cb) { return register(KONAMI, cb); }

// Any custom key sequence (array of e.key values).
function onSequence(keys, cb) { return register(keys, cb); }

// A typed word, e.g. onCode('xyzzy', fn) -> matches when typed in order.
function onCode(word, cb) { return register(String(word).split(''), cb); }

// For testing / programmatic triggering: feed a key as if pressed.
function feed(key) { onKeydown({ key: key }); }

function reset() { watchers.forEach(function (w) { w.buffer = []; }); }

window.weld.konami = {
  onKonami: onKonami,
  onSequence: onSequence,
  onCode: onCode,
  feed: feed,
  reset: reset,
  KONAMI: KONAMI.slice(),
  version: '1.0.0'
};

return window.weld.konami;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
