$meta
  title       = weld.registry examples
  description = Interactive demo of weld.registry -- a client for the public generator API. Look up stats, download source, resolve dependencies, list generators, and build screenshot URLs.
  tags        = weld, registry, api, generator, example, plugin

  weldRegistry = {import:weld-registry-plugin}
  weldFetch    = {import:weld-fetch-plugin}
  superFetch   = {import:super-fetch-plugin}
