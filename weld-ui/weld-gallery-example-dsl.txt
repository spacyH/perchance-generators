$meta
  title       = weld.gallery examples
  description = Image grid demo with IndexedDB persistence: add gradient images, sort, favorite, lightbox. Data persists across reloads.
  tags        = weld, gallery, image, cache, example, plugin

weldUi      = {import:weld-ui-plugin}
weldGallery = {import:weld-gallery-plugin}
