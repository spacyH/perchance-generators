$meta
  title = weld.ephemeral examples
  description = Interactive demo of weld.ephemeral -- preview, arm a confirm token, and clear all weld-managed local storage (IndexedDB + localStorage) for this generator. Local-only; does not touch server data.
  tags = weld, ephemeral, privacy, clear, example, plugin

  weldEphemeral = {import:weld-ephemeral-plugin}
