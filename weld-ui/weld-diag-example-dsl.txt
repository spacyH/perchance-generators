$meta
  title       = weld.diag examples
  description = Interactive demo of weld.diag -- lint plugin code for brace-interception traps and common bugs, and use the structured logger/bundler. The Weld suite's own quality gate.
  tags        = weld, diag, lint, debug, logger, example, plugin

weldDiag = {import:weld-diag-plugin}
