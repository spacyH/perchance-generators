$meta
  title       = weld.embed examples
  description = Interactive demo of weld.embed -- local semantic search. Type candidate texts and a query, and rank them by meaning in the browser with cosine similarity. Falls back to a lexical engine if the model can't load.
  tags        = weld, embed, semantic, search, example, plugin

  weldEmbed = {import:weld-embed-plugin}
