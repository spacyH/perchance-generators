$meta
  title       = weld.backup — Examples
  description = Live demo of weld.backup: export a sample dataset, restore it back, validate a file before importing, and watch format migrations transform a v1 backup into the current schema.
  tags        = weld, backup, export, import, example, plugin

weldUi      = {import:weld-ui-plugin}
weldMigrate = {import:weld-migrate-plugin}
weldBackup  = {import:weld-backup-plugin}
