<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{
  --bg:#0d1117; --surface:#161b22; --ink:#e6edf3; --muted:#7d8590;
  --faint:#21262d; --accent:#4493f8; --green:#3fb950; --red:#f85149;
  --yellow:#d29922; --border:rgba(255,255,255,0.10);
  --font:'DM Sans',system-ui,sans-serif;
  --mono:'JetBrains Mono',monospace;
}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.65 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}
a:hover{text-decoration:underline;}
#page{max-width:720px;margin:0 auto;padding:48px 24px 96px;}
.hdr{margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;}
.hdr h1{font-size:30px;font-weight:700;letter-spacing:-0.5px;font-family:var(--mono);}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);
  padding:2px 8px;border-radius:4px;letter-spacing:0.5px;}
.hdr .lead{color:var(--muted);font-size:15px;max-width:640px;}
h2{font-size:20px;font-weight:600;margin:48px 0 16px;letter-spacing:-0.2px;
  padding-top:24px;border-top:1px solid var(--border);}
h2:first-of-type{border-top:0;padding-top:0;}
h3{font-size:15px;font-weight:600;margin:28px 0 10px;color:var(--ink);font-family:var(--mono);}
h4{font-size:13px;font-weight:600;margin:20px 0 8px;color:var(--muted);
  letter-spacing:0.5px;text-transform:uppercase;font-family:var(--mono);}
p{margin-bottom:14px;color:#c9d1d9;}
strong{color:var(--ink);font-weight:600;}
code{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:1px 6px;border-radius:3px;}
pre{background:var(--faint);border:1px solid var(--border);border-radius:6px;
  padding:14px 16px;overflow-x:auto;margin:14px 0;font-family:var(--mono);
  font-size:12.5px;line-height:1.55;color:#c9d1d9;min-height:1.5em;}
pre code{background:none;padding:0;color:inherit;font-size:inherit;}
.c-c{color:var(--muted);}
.c-k{color:#ff7b72;}
.c-s{color:#a5d6ff;}
.c-n{color:#d2a8ff;}
.c-f{color:#79c0ff;}
.sig{font-family:var(--mono);font-size:13px;color:var(--accent);
  background:var(--faint);padding:8px 12px;border-radius:4px;
  border-left:3px solid var(--accent);margin:8px 0 12px;display:block;
  min-height:1.5em;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);
  padding:12px 16px;border-radius:0 4px 4px 0;margin:14px 0;font-size:14px;color:#c9d1d9;}
.warn{background:rgba(210,153,34,0.07);border-left-color:var(--yellow);}
table{width:100%;border-collapse:collapse;margin:14px 0;font-size:13px;}
th,td{text-align:left;padding:8px 10px;border-bottom:1px solid var(--border);}
th{color:var(--muted);font:600 11px var(--mono);text-transform:uppercase;letter-spacing:1px;}
td{color:#c9d1d9;}
td code{font-size:12px;}
ul{list-style:none;padding:0;margin:14px 0;}
li{padding:4px 0;color:#c9d1d9;}
li::before{content:'>';color:var(--muted);font-family:var(--mono);margin-right:10px;font-size:11px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;
  font:12px var(--mono);color:var(--muted);padding:14px 0 18px;margin-bottom:28px;
  border-bottom:1px solid var(--border);}
.breadcrumb .crumb a{color:var(--muted);}
.breadcrumb .crumb a:hover{color:var(--accent);}
.breadcrumb .crumb-sep{color:var(--faint);margin:0 8px;}
.breadcrumb .crumb-cur{color:var(--ink);}
.breadcrumb .crumb-action a{color:var(--accent);}
.footer{text-align:center;font-family:var(--mono);max-width:720px;margin:64px auto 24px;padding:24px;border-top:1px solid var(--border);color:var(--muted);font-size:12px;}
.footer span,.footer a{color:var(--muted);}
.footer a{margin:0 2px;}
.footer a:hover{color:var(--accent);}
</style>
<div id="page">
<div class="breadcrumb"><span class="crumb"><a href="https://perchance.org/weld-ui-index">weld</a><span class="crumb-sep">/</span>weld.import<span class="crumb-sep">/</span><span class="crumb-cur">docs</span></span><span class="crumb-action"><a href="https://perchance.org/weld-import-example">demo &rarr;</a></span></div>
<div class="hdr">
  <div class="hdr-row">
      <h1>weld.import</h1>
      <span class="ver">v1.0.0</span>
  </div>
  <p class="lead">A format-detecting importer for character and chat data: TavernAI/SillyTavern cards (v1/v2/v3, JSON or PNG), FurAI, ai-character-chat, and JSONL, all normalized to one shape. Routes PNG cards through weld.cards.</p>
</div>
<h2>One front door for foreign data</h2>
<p>Every community tool exports characters and chats in its own shape. weld.import sniffs which one you handed it and normalizes the result, so the rest of your app never branches on format. It generalizes the import suite built for Smart-story and uses <code>weld.cards</code> as the backend for PNG character cards.</p>
<pre data-codeid="install"></pre>

<h2>detect(input)</h2>
<div class="sig" data-codeid="sig_detect"></div>
<p>A fast, non-destructive sniff. Returns <code>\&#123; format, kind, confidence \&#125;</code> where <code>kind</code> is <code>'character'</code>, <code>'chat'</code>, or <code>'unknown'</code>. Use it to show the user what you detected before committing.</p>
<pre data-codeid="detect_ex"></pre>
<table>
<tr><th>format</th><th>kind</th><th>source</th></tr>
<tr><td><code>tavern-png</code></td><td>character</td><td>character card embedded in a PNG (via weld.cards)</td></tr>
<tr><td><code>tavern-v3 / v2 / v1</code></td><td>character</td><td>TavernAI / SillyTavern JSON cards</td></tr>
<tr><td><code>furai-character</code></td><td>character</td><td>FurAI character export</td></tr>
<tr><td><code>furai-chat / generic-chat</code></td><td>chat</td><td>message arrays / <code>\&#123; messages \&#125;</code></td></tr>
<tr><td><code>ai-character-chat</code></td><td>chat / character</td><td>ai-character-chat dumps</td></tr>
<tr><td><code>jsonl-chat</code></td><td>chat</td><td>newline-delimited JSON messages</td></tr>
</table>

<h2>parse(input) / parseFile(file)</h2>
<div class="sig" data-codeid="sig_parse"></div>
<p>Returns a Promise of <code>\&#123; format, kind, character?, chat?, warnings \&#125;</code>. A <strong>character</strong> uses the same fields as <code>weld.cards.toCharacter</code> (name, description, personality, scenario, first_message, example_dialogue, …). A <strong>chat</strong> is <code>\&#123; title, messages:\[\&#123; role, name, content \&#125;\] \&#125;</code> with the many "who spoke" conventions (<code>role</code>, <code>is_user</code>, <code>name</code>) all mapped to <code>role</code>.</p>
<pre data-codeid="parse_ex"></pre>
<p><code>parseFile()</code> reads an uploaded <code>File</code>/<code>Blob</code> first &mdash; PNG as bytes (routed to weld.cards), everything else as text.</p>
<pre data-codeid="file_ex"></pre>
<div class="note">PNG character cards require <code>weld.cards</code> (import it or <code>attach()</code> it). Malformed lines in a JSONL log are skipped with a warning rather than failing the whole import. Pair with <code>weld.persona</code> or your character store to save the normalized result.</div>
<script>
(function(){
  var examples = {
  'install': 'output\n  weld-import = \{import:weld-import-plugin\}\n  weldCards   = \{import:weld-cards-plugin\}   // for PNG cards',
  'sig_detect': 'weld.import.detect(input) -> \{ format, kind, confidence \}',
  'detect_ex': 'var d = weld.import.detect(pastedText);\nif (d.kind === \'character\') showPreview(\'Looks like a \' + d.format + \' card\');',
  'sig_parse': 'weld.import.parse(input) -> Promise<\{ format, kind, character?, chat?, warnings \}>',
  'parse_ex': 'var r = await weld.import.parse(pastedText);\nif (r.character) library.add(r.character);\nelse if (r.chat) loadConversation(r.chat.messages);\nr.warnings.forEach(w => console.warn(w));',
  'sig_file': 'weld.import.parseFile(file) -> Promise<result>   //  weld.import.formats() -> string\[]',
  'file_ex': 'picker.onchange = async (e) => \{\n  var r = await weld.import.parseFile(e.target.files\[0]);\n  if (r.character) library.add(r.character);\n\};'
};
  function escapeHTML(s){
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  }
  function highlight(code){
    code = escapeHTML(code);
    code = code.replace(/(\/\/[^\n]*)/g, '<span class="c-c">$1</span>');
    code = code.replace(/('[^'\n]*')/g, '<span class="c-s">$1</span>');
    code = code.replace(/("[^"\n]*")/g, '<span class="c-s">$1</span>');
    var kws = ['var','let','const','function','return','if','else','for','while','await','async','new','typeof','instanceof','null','true','false','this','of','in'];
    code = code.replace(new RegExp('\\b(' + kws.join('|') + ')\\b', 'g'), '<span class="c-k">$1</span>');
    code = code.replace(/\b(\d+(?:\.\d+)?)\b/g, '<span class="c-n">$1</span>');
    return code;
  }
  function fill(){
    document.querySelectorAll('[data-codeid]').forEach(function(n){
      var src = examples[n.getAttribute('data-codeid')];
      if (src == null) return;
      if (n.tagName === 'PRE') n.innerHTML = highlight(src);
      else n.textContent = src;
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fill);
  else fill();
})();
</script>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.import) return window.weld.import;

// -----------------------------------------------------------------
// weld.import: one front door for "here's a character/chat from
// somewhere else, load it." Every community tool has its own export
// shape; this detects which one you handed it and returns a normalized
// result so the rest of your app never branches on format:
//
//   var r = weld.import.detect(pastedText);     // { format, kind, confidence }
//   var out = await weld.import.parse(input);   // { format, character?, chat?, warnings }
//   var out = await weld.import.parseFile(file);// same, for an uploaded File/Blob
//
// kind is 'character' | 'chat' | 'unknown'. A character normalizes to the
// same fields weld.cards.toCharacter produces; a chat normalizes to
// { messages:[{ role, name, content }], title }. PNG character cards are
// delegated to weld.cards when it's present.
// -----------------------------------------------------------------

var deps = { cards: null };
function attach(d) { if (d && d.cards) deps.cards = d.cards; }
function cards() { return deps.cards || (window.weld && window.weld.cards) || null; }

// ---- helpers ----
function asObj(input) {
  if (input && typeof input === 'object') return input;
  if (typeof input === 'string') { try { return JSON.parse(input); } catch (e) { return null; } }
  return null;
}
function has(o) { for (var i = 1; i < arguments.length; i++) { if (o && o[arguments[i]] !== undefined) return true; } return false; }
function str(v) { return v == null ? '' : String(v); }

// ---- format detection -------------------------------------------------
// Returns { format, kind, confidence } without fully parsing.
function detect(input) {
  // PNG bytes (character card embedded in image)
  if (looksLikePng(input)) return { format: 'tavern-png', kind: 'character', confidence: 0.95 };

  var text = (typeof input === 'string') ? input : null;
  var o = asObj(input);

  // JSONL: multiple lines, each a JSON object (chat log)
  if (text && isJsonl(text)) return { format: 'jsonl-chat', kind: 'chat', confidence: 0.8 };

  if (!o) return { format: 'unknown', kind: 'unknown', confidence: 0 };

  // TavernAI / SillyTavern character card v2/v3 (spec + data envelope)
  if (o.spec === 'chara_card_v3' || (o.spec_version && /3/.test(str(o.spec_version)) && o.data)) {
    return { format: 'tavern-v3', kind: 'character', confidence: 0.95 };
  }
  if (o.spec === 'chara_card_v2' || (o.spec_version && o.data)) {
    return { format: 'tavern-v2', kind: 'character', confidence: 0.95 };
  }
  // ai-character-chat export (its own envelope)
  if (has(o, 'addCharacter', 'characterName') || (o.characters && o.messages && o.threadName !== undefined)) {
    return { format: 'ai-character-chat', kind: (o.messages ? 'chat' : 'character'), confidence: 0.7 };
  }
  // FurAI character (flat fields, FurAI-specific keys)
  if (has(o, 'persona', 'greeting') && (o.char_name || o.name)) {
    return { format: 'furai-character', kind: 'character', confidence: 0.6 };
  }
  // FurAI / generic chat dump (array of messages or {messages:[...]})
  if (Array.isArray(o) && o.length && (o[0].role || o[0].is_user !== undefined || o[0].mes !== undefined)) {
    return { format: 'furai-chat', kind: 'chat', confidence: 0.6 };
  }
  if (Array.isArray(o.messages) && o.messages.length) {
    return { format: 'generic-chat', kind: 'chat', confidence: 0.55 };
  }
  // TavernAI v1 / generic flat character card
  if (has(o, 'name', 'char_name') && has(o, 'description', 'personality', 'first_mes', 'persona')) {
    return { format: 'tavern-v1', kind: 'character', confidence: 0.55 };
  }
  return { format: 'unknown', kind: 'unknown', confidence: 0 };
}

function looksLikePng(input) {
  var c = cards();
  if (c && typeof c.isPng === 'function') { try { return c.isPng(input); } catch (e) {} }
  // fallback signature check on a byte-ish input
  try {
    var b = input;
    if (b && b.length >= 8 && b[0] === 0x89 && b[1] === 0x50 && b[2] === 0x4E && b[3] === 0x47) return true;
  } catch (e) {}
  return false;
}
function isJsonl(text) {
  var lines = text.split(/\r?\n/).filter(function (l) { return l.trim(); });
  if (lines.length < 2) return false;
  var okCount = 0, checked = 0;
  for (var i = 0; i < lines.length && checked < 6; i++) {
    checked++;
    try { var o = JSON.parse(lines[i]); if (o && typeof o === 'object') okCount++; } catch (e) { /* tolerate a bad line */ }
  }
  // It's JSONL if most sampled lines parse as objects (allows a stray bad line).
  return okCount >= 2 && okCount >= (checked - 1);
}

// ---- normalizers ------------------------------------------------------
function normCharacterFlat(d) {
  return {
    name: str(d.name || d.char_name),
    description: str(d.description || d.persona),
    personality: str(d.personality),
    scenario: str(d.scenario || d.world_scenario),
    first_message: str(d.first_mes || d.first_message || d.greeting),
    example_dialogue: str(d.mes_example || d.example_dialogue),
    system_prompt: str(d.system_prompt),
    creator_notes: str(d.creator_notes),
    tags: Array.isArray(d.tags) ? d.tags : [],
    creator: str(d.creator)
  };
}
function normMessage(m, idx) {
  // map the many "who said it" conventions to role/name/content
  var role, name = '', content = '';
  if (m.role) role = (m.role === 'assistant' || m.role === 'char') ? 'assistant' : (m.role === 'user' ? 'user' : m.role);
  else if (m.is_user !== undefined) role = m.is_user ? 'user' : 'assistant';
  else if (m.name && /you|user/i.test(m.name)) role = 'user';
  else role = 'assistant';
  name = str(m.name || m.author || m.speaker);
  content = str(m.content || m.mes || m.text || m.message);
  return { role: role, name: name, content: content };
}
function normChatMessages(arr) {
  return (arr || []).map(normMessage).filter(function (m) { return m.content; });
}

// ---- parse ------------------------------------------------------------
// parse(input) -> Promise<{ format, kind, character?, chat?, warnings }>
function parse(input) {
  var det = detect(input);
  var warnings = [];
  try {
    switch (det.format) {
      case 'tavern-png': {
        var c = cards();
        if (!c || typeof c.read !== 'function') return Promise.reject(new Error('weld.import: PNG card needs weld.cards (import it / attach it)'));
        var spec = c.read(input);
        if (!spec) { warnings.push('PNG had no embedded character data'); return Promise.resolve({ format: det.format, kind: 'unknown', warnings: warnings }); }
        return Promise.resolve({ format: det.format, kind: 'character', character: c.toCharacter(spec.spec || spec), warnings: warnings });
      }
      case 'tavern-v3':
      case 'tavern-v2': {
        var o = asObj(input);
        var d = (o && o.data) ? o.data : o;
        // prefer weld.cards.toCharacter when present (handles spec quirks)
        var c2 = cards();
        var ch = (c2 && c2.toCharacter) ? c2.toCharacter(o) : normCharacterFlat(d);
        return Promise.resolve({ format: det.format, kind: 'character', character: ch, warnings: warnings });
      }
      case 'tavern-v1':
      case 'furai-character': {
        return Promise.resolve({ format: det.format, kind: 'character', character: normCharacterFlat(asObj(input)), warnings: warnings });
      }
      case 'ai-character-chat': {
        var acc = asObj(input);
        if (acc && Array.isArray(acc.messages)) {
          return Promise.resolve({ format: det.format, kind: 'chat', chat: { title: str(acc.threadName || acc.title), messages: normChatMessages(acc.messages) }, warnings: warnings });
        }
        return Promise.resolve({ format: det.format, kind: 'character', character: normCharacterFlat(acc.character || acc), warnings: warnings });
      }
      case 'furai-chat':
      case 'generic-chat': {
        var go = asObj(input);
        var arr = Array.isArray(go) ? go : go.messages;
        return Promise.resolve({ format: det.format, kind: 'chat', chat: { title: str(go.title || go.threadName), messages: normChatMessages(arr) }, warnings: warnings });
      }
      case 'jsonl-chat': {
        var lines = String(input).split(/\r?\n/).filter(function (l) { return l.trim(); });
        var msgs = [];
        for (var i = 0; i < lines.length; i++) { try { msgs.push(JSON.parse(lines[i])); } catch (e) { warnings.push('skipped malformed line ' + (i + 1)); } }
        return Promise.resolve({ format: det.format, kind: 'chat', chat: { title: '', messages: normChatMessages(msgs) }, warnings: warnings });
      }
      default:
        return Promise.resolve({ format: 'unknown', kind: 'unknown', warnings: ['weld.import: unrecognized format'] });
    }
  } catch (e) {
    return Promise.reject(new Error('weld.import: parse failed (' + (e && e.message) + ')'));
  }
}

// parseFile(file) -> reads a File/Blob then parses (PNG as bytes, else text).
function parseFile(file) {
  if (!file) return Promise.reject(new Error('weld.import: no file'));
  var isPngName = /\.png$/i.test(file.name || '') || (file.type === 'image/png');
  return new Promise(function (resolve, reject) {
    if (typeof FileReader === 'undefined') { reject(new Error('weld.import: FileReader unavailable')); return; }
    var fr = new FileReader();
    fr.onload = function () { resolve(fr.result); };
    fr.onerror = function () { reject(new Error('weld.import: file read failed')); };
    if (isPngName) fr.readAsArrayBuffer(file); else fr.readAsText(file);
  }).then(function (data) {
    if (isPngName) return parse(new Uint8Array(data));
    return parse(data);
  });
}

function formats() {
  return ['tavern-png', 'tavern-v3', 'tavern-v2', 'tavern-v1', 'furai-character', 'furai-chat', 'ai-character-chat', 'generic-chat', 'jsonl-chat'];
}

window.weld.import = {
  detect: detect,
  parse: parse,
  parseFile: parseFile,
  attach: attach,
  formats: formats,
  version: '1.0.0'
};

return window.weld.import;
})();
</script>
<footer class="footer">
  <span>&#169; 2026 therealwestninja.</span> &#183;
  <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183;
  <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a>
</footer>
</div>
