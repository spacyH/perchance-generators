$meta
  title       = weld.stream examples
  description = Interactive demo for weld.stream -- live streaming generation against ai-text-plugin, abort mid-stream, the single-broker serial queue, and live token budgeting.
  tags        = weld, stream, ai, generation, example, plugin

weldUi       = {import:weld-ui-plugin}
weldStream   = {import:weld-stream-plugin}
aiTextPlugin = {import:ai-text-plugin}
