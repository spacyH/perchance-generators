$meta
  title = weld.privacy examples
  description = Interactive demo of weld.privacy -- declare and display, per backend, how data is handled (local-only, anonymized, server-stored, public, expiring, third-party), with honest pre-registered claims for the suite's own backends.
  tags = weld, privacy, data, transparency, example, plugin

  weldPrivacy = {import:weld-privacy-plugin}
