$meta
  title       = weld.cache examples
  description = Interactive demo of weld.cache -- semantic response caching. Ask a question, then ask it differently; the cached answer is reused when meanings match.
  tags        = weld, cache, semantic, memoize, example, plugin

  weldEmbed = {import:weld-embed-plugin}
  weldCache = {import:weld-cache-plugin}
