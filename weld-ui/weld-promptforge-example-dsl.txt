$meta
  title       = weld.promptforge examples
  description = Interactive demo of weld.promptforge -- evolve image prompts by taste. Generate candidates, upvote good ones, and watch winners breed into the next round with score-weighted promotion.
  tags        = weld, promptforge, prompt, evolution, example, plugin

  weldStream      = {import:weld-stream-plugin}
  weldStructured  = {import:weld-structured-plugin}
  weldPromptforge = {import:weld-promptforge-plugin}
  aiTextPlugin    = {import:ai-text-plugin}
