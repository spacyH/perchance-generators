<style>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');
:root{--bg:#0d1117;--surface:#161b22;--ink:#e6edf3;--muted:#7d8590;--faint:#21262d;--accent:#4493f8;--green:#3fb950;--red:#f85149;--amber:#d29922;--border:rgba(255,255,255,0.10);--font:'DM Sans',system-ui,sans-serif;--mono:'JetBrains Mono',monospace;}
*{box-sizing:border-box;margin:0;}
body{background:var(--bg);color:var(--ink);font:15px/1.6 var(--font);-webkit-font-smoothing:antialiased;}
a{color:var(--accent);text-decoration:none;}a:hover{text-decoration:underline;}
#page{max-width:780px;margin:0 auto;padding:34px 18px 90px;}
.breadcrumb{display:flex;justify-content:space-between;align-items:center;font:12px var(--mono);color:var(--muted);padding:12px 0 16px;margin-bottom:20px;border-bottom:1px solid var(--border);}
.breadcrumb a{color:var(--muted);}.breadcrumb .sep{color:var(--faint);margin:0 8px;}.breadcrumb .cur{color:var(--ink);}.breadcrumb .act a{color:var(--accent);}
.hdr{margin-bottom:20px;padding-bottom:14px;border-bottom:1px solid var(--border);}
.hdr-row{display:flex;align-items:baseline;gap:12px;flex-wrap:wrap;}.hdr h1{font:700 24px var(--mono);letter-spacing:-0.5px;}
.hdr .ver{font:600 11px var(--mono);color:var(--muted);background:var(--faint);padding:2px 8px;border-radius:4px;}
.hdr .lead{color:var(--muted);margin-top:8px;}
.panel{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:16px;margin:14px 0;}
.panel h2{font:600 13px var(--mono);color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:12px;}
.row{display:flex;align-items:center;gap:9px;flex-wrap:wrap;margin:8px 0;}
.lbl{font:600 12px var(--mono);color:var(--muted);min-width:74px;}
.btn{background:var(--accent);color:#fff;border:0;border-radius:6px;padding:9px 15px;font:600 13px var(--font);cursor:pointer;transition:.12s;}
.btn:hover{filter:brightness(1.08);}.btn.ghost{background:var(--faint);color:var(--ink);}.btn.sm{padding:6px 11px;font-size:12px;}
.btn:disabled{opacity:.4;cursor:not-allowed;}
input[type=text],textarea{background:var(--bg);border:1px solid var(--border);border-radius:6px;color:var(--ink);padding:8px 10px;font:13px var(--font);}
input[type=text]{flex:1;min-width:120px;}textarea{width:100%;font:12.5px/1.5 var(--mono);resize:vertical;}
.mono{font:13px var(--mono);}
.out{background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:12px 14px;font:12.5px/1.55 var(--mono);white-space:pre-wrap;word-break:break-word;min-height:20px;color:#c9d1d9;}
.chip{display:inline-block;background:var(--faint);border:1px solid var(--border);border-radius:6px;padding:3px 9px;font:600 12px var(--mono);margin:2px;}
.chip.k{background:rgba(63,185,80,.14);border-color:rgba(63,185,80,.4);color:#7ee787;}
.chip.d{opacity:.4;text-decoration:line-through;}
.note{background:rgba(68,147,248,0.07);border-left:3px solid var(--accent);padding:9px 13px;border-radius:0 4px 4px 0;margin:12px 0 0;font-size:12.5px;color:#c9d1d9;}
.note.warn{background:rgba(210,153,34,.08);border-color:var(--amber);}
.tag{font:600 10px var(--mono);padding:2px 7px;border-radius:4px;background:rgba(63,185,80,.14);color:#7ee787;border:1px solid rgba(63,185,80,.4);}
.tag.ai{background:rgba(210,153,34,.12);color:#e3b341;border-color:rgba(210,153,34,.4);}
.bar{height:14px;background:var(--accent);border-radius:3px;transition:width .3s;}
.footer{margin-top:40px;padding-top:16px;border-top:1px solid var(--border);font:12px var(--mono);color:var(--muted);}.footer a{color:var(--muted);}
.flex{display:flex;gap:10px;align-items:center;}.wrap{flex-wrap:wrap;}.grow{flex:1;}.mt{margin-top:10px;}
</style>
<div id="page">
  <div class="breadcrumb"><span><a href="https://perchance.org/weld-ui-index">weld</a><span class="sep">/</span>weld.clean<span class="sep">/</span><span class="cur">demo</span></span><span class="act"><a href="https://perchance.org/weld-clean-plugin">docs &rarr;</a></span></div>
  <div class="hdr"><div class="hdr-row"><h1>weld.clean &mdash; demo</h1><span class="ver">v1.0.0</span></div><p class="lead">Scrub raw model text before you store or render it: trim a dangling sentence after truncation, strip role-name bleed, collapse repetition, and balance code fences. Every fixer runs purely in the browser.</p></div>

  <div class="panel"><h2>Raw model output <span class="tag">pure</span></h2>
    <textarea id="craw" rows="9">Aria: Of course! Here is what you asked for:

```js
const greeting = "hello"
console.log(greeting)

The castle stood against against against the storm, its towers lost in cloud. The wind howled and howled and howled. She turned to leave, but then she saw a fla</textarea>
    <div class="row mt"><span class="lbl">role names</span><input id="croles" type="text" value="Aria, Bot, Assistant"><label class="flex" style="gap:5px"><input id="ctrunc" type="checkbox" checked> truncated</label></div>
    <div class="row"><button id="cclean" class="btn">Clean</button>
      <button class="btn ghost sm cf" data-fn="trimPartial">trimPartial</button>
      <button class="btn ghost sm cf" data-fn="stripRolePrefix">stripRolePrefix</button>
      <button class="btn ghost sm cf" data-fn="collapseRepeats">collapseRepeats</button>
      <button class="btn ghost sm cf" data-fn="balanceFences">balanceFences</button></div>
    <div class="mt"><div class="lbl mono">result:</div><div id="cout" class="out mt"></div></div>
    <div class="note">clean() runs the whole pipeline; with <code>truncated</code> checked it also trims the dangling half-sentence (the way a cut-off weld.stream result arrives). The individual buttons show each fixer in isolation.</div></div>
  <footer class="footer"><span>&#169; 2026 therealwestninja.</span> &#183; <a href="https://www.deviantart.com/west-ninja" target="_blank" rel="noopener">DeviantArt</a> &#183; <a href="https://github.com/therealwestninja" target="_blank" rel="noopener">GitHub</a></footer>
</div>
<script>
(function(){
'use strict';

if (!window.weld) window.weld = {};
if (window.weld.clean) return window.weld.clean;

// -----------------------------------------------------------------
// weld.clean: the "scrub the raw model text" step every chat app
// re-implements. A text model returns prose that often needs fixing
// before you store or render it:
//   - a half-finished trailing sentence when the output was truncated
//   - the model echoing "Name:" or hallucinating the next speaker turn
//   - repetition loops (the same line/word over and over)
//   - an unclosed ``` code fence that breaks the markdown renderer
//   - leftover hidden-from-AI marker blocks
// Each fixer is exposed on its own; clean() runs a configurable pipe.
// Everything here is pure string -> string, no dependencies.
// -----------------------------------------------------------------

var HIDDEN_START = '<!--hidden-from-ai-start-->';
var HIDDEN_END   = '<!--hidden-from-ai-end-->';
var FENCE = String.fromCharCode(96, 96, 96); // three backticks

// Remove an incomplete final sentence. Use when a result was truncated
// (stopReason "artificial") so you don't show a chopped-off fragment.
function trimPartial(text) {
  var s = String(text == null ? '' : text);
  if (!s) return s;
  var trimmed = s.replace(/\s+$/, '');
  // Already ends cleanly on terminal punctuation or a closing quote/bracket.
  if (/[.!?\u2026\u3002\uff01\uff1f]["'\u201d\u2019)\]]?$/.test(trimmed)) return trimmed;
  // Otherwise cut back to the last sentence terminator we can find.
  var m = trimmed.match(/[\s\S]*[.!?\u2026\u3002\uff01\uff1f]["'\u201d\u2019)\]]?/);
  if (m && m[0].trim().length > 0) return m[0].replace(/\s+$/, '');
  return trimmed; // no terminator at all: leave it as-is rather than blank it
}

// Strip a leading "Name:" the model echoed, and cut the reply at the
// point where it starts speaking AS another turn (role bleed).
// names: optional array of speaker names to police; if omitted, any
// capitalized "Word:" or "Word :" at the start of a line is treated
// as a turn boundary.
function stripRolePrefix(text, names) {
  var s = String(text == null ? '' : text);
  if (!s) return s;

  function speakerAlt() {
    if (names && names.length) {
      var esc = names.map(function (n) {
        return String(n).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
      });
      return '(?:' + esc.join('|') + ')';
    }
    return '[A-Z][\\w .\u2019\'-]{0,30}';
  }
  var alt = speakerAlt();

  // 1. Drop a single leading "Name:" prefix on the whole reply.
  s = s.replace(new RegExp('^\\s*' + alt + '\\s*:\\s*'), '');

  // 2. Cut at the first mid-reply line that starts a new speaker turn.
  var boundary = new RegExp('\\n\\s*' + alt + '\\s*:\\s');
  var hit = s.search(boundary);
  if (hit !== -1) s = s.slice(0, hit);

  return s.replace(/\s+$/, '');
}

// Collapse runaway repetition: identical adjacent lines, identical
// adjacent sentences, and long single-character runs.
function collapseRepeats(text, opts) {
  opts = opts || {};
  var maxLineRepeat = opts.maxLineRepeat || 2;
  var maxCharRun = opts.maxCharRun || 8;
  var s = String(text == null ? '' : text);
  if (!s) return s;

  // Long single-character runs (e.g. "okayyyyyyyy", "!!!!!!!!").
  s = s.replace(/(.)\1{7,}/g, function (m, ch) {
    return new Array(maxCharRun + 1).join(ch);
  });

  // Identical adjacent lines beyond the allowed repeat count.
  var lines = s.split('\n');
  var out = [];
  var run = 0, prev = null;
  for (var i = 0; i < lines.length; i++) {
    var key = lines[i].trim();
    if (key !== '' && key === prev) {
      run += 1;
      if (run >= maxLineRepeat) continue;
    } else {
      run = 0; prev = key;
    }
    out.push(lines[i]);
  }
  return out.join('\n');
}

// Ensure triple-backtick code fences are balanced. If an odd fence is
// left open, append a closing one so the markdown renderer recovers.
function balanceFences(text) {
  var s = String(text == null ? '' : text);
  var count = 0, idx = s.indexOf(FENCE);
  while (idx !== -1) { count += 1; idx = s.indexOf(FENCE, idx + 3); }
  if (count % 2 === 1) {
    if (!/\n$/.test(s)) s += '\n';
    s += FENCE;
  }
  return s;
}

// Remove hidden-from-AI marker blocks and their contents.
function stripHidden(text) {
  var s = String(text == null ? '' : text);
  var start = s.indexOf(HIDDEN_START);
  while (start !== -1) {
    var end = s.indexOf(HIDDEN_END, start);
    if (end === -1) { s = s.slice(0, start); break; }
    s = s.slice(0, start) + s.slice(end + HIDDEN_END.length);
    start = s.indexOf(HIDDEN_START);
  }
  return s;
}

// Apply an ordered list of find/replace rules.
// rule = { find: string|RegExp, replace: string, flags?: string }
function applyRules(text, ruleList) {
  var s = String(text == null ? '' : text);
  if (!ruleList || !ruleList.length) return s;
  for (var i = 0; i < ruleList.length; i++) {
    var r = ruleList[i];
    if (!r || r.find == null) continue;
    var re;
    if (r.find instanceof RegExp) re = r.find;
    else re = new RegExp(String(r.find), r.flags == null ? 'g' : r.flags);
    s = s.replace(re, r.replace == null ? '' : String(r.replace));
  }
  return s;
}

// The pipeline. Each stage is opt-in; pass a result object straight
// from weld.stream and clean() will trimPartial only when truncated.
// opts:
//   truncated     (bool)    run trimPartial (auto-true if you pass result)
//   roleNames     (array)   names for stripRolePrefix; [] enables generic mode
//   roles         (bool)    enable stripRolePrefix even without names
//   collapse      (bool)    collapseRepeats (default true)
//   fences        (bool)    balanceFences (default true)
//   hidden        (bool)    stripHidden (default true)
//   rules         (array)   custom find/replace rules
//   trim          (bool)    trim outer whitespace (default true)
function clean(input, opts) {
  opts = opts || {};
  var text, truncated;
  if (input && typeof input === 'object' && 'text' in input) {
    // A weld.stream result object.
    text = input.text;
    truncated = (opts.truncated != null) ? opts.truncated : !!input.truncated;
  } else {
    text = input;
    truncated = !!opts.truncated;
  }
  var s = String(text == null ? '' : text);

  if (opts.hidden !== false) s = stripHidden(s);
  if (opts.roles || (opts.roleNames != null)) s = stripRolePrefix(s, opts.roleNames);
  if (opts.collapse !== false) s = collapseRepeats(s, opts);
  if (truncated) s = trimPartial(s);
  if (opts.rules) s = applyRules(s, opts.rules);
  if (opts.fences !== false) s = balanceFences(s);
  if (opts.trim !== false) s = s.replace(/^\s+|\s+$/g, '');
  return s;
}

window.weld.clean = {
  clean: clean,
  trimPartial: trimPartial,
  stripRolePrefix: stripRolePrefix,
  collapseRepeats: collapseRepeats,
  balanceFences: balanceFences,
  stripHidden: stripHidden,
  rules: applyRules,
  version: '1.0.0'
};

return window.weld.clean;
})();
</script>
<script>
(function(){
  function $(id){return document.getElementById(id);}
  function boot(){ if(!window.weld||!window.weld.clean){return setTimeout(boot,100);} init(); }
  function init(){
    var C=window.weld.clean;
    function roles(){ return $('croles').value.split(',').map(function(s){return s.trim();}).filter(Boolean); }
    $('cclean').onclick=function(){ var r={text:$('craw').value, truncated:$('ctrunc').checked}; $('cout').textContent=C.clean(r,{roleNames:roles()}); };
    [].forEach.call(document.querySelectorAll('.cf'),function(b){ b.onclick=function(){ var fn=b.getAttribute('data-fn'),t=$('craw').value,out;
      if(fn==='stripRolePrefix') out=C.stripRolePrefix(t,roles());
      else out=C[fn](t);
      $('cout').textContent=out; }; });
    $('cclean').click();
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',boot); else boot();
})();
</script>
