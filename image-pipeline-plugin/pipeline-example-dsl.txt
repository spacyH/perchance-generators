$meta
  title       = Custom Image Pipeline — Examples
  description = Interactive demos for custom-image-pipeline-plugin. Batch generation, style presets, resolution presets, retry on failure, and async Promise-based image generation.
  tags        = plugin, example, image, pipeline, demo

pipeline = {import:custom-image-pipeline-plugin}
