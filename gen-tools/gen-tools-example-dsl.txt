$meta
  title       = Gen Tools Plugin — Examples
  description = Live demos for gen-tools-plugin. Explore any generator's lists and source code, search across items, compare two generators side-by-side, and modify the DSL tree at runtime.
  tags        = plugin, example, demo, tools, generator, inspect

dynamicImport = {import:dynamic-import-plugin}
superFetch    = {import:super-fetch-plugin}
