<!-- [window.events = [], getEvents(), ''] -->
<p style="text-align:left; margin-left:0.5rem;">card1:</p>
<p>[card1]</p>

<!-- 
  Note: Below is a copy of the CSS from the /generators page
  just so it's easy to preview what it'll look like in the output
  above. Changing this CSS has no effect on the way it's actually
  displayed on the /generators page.
  If the CSS on /generators changes in the future, then the above
  preview won't be accurate, but I'll let a hub member know of any
  changes to the card CSS in advance.
-->
<style>
  html {
    color-scheme: light dark;
  }
  table.generator-card {
    width: 270px;
    height: 180px;
    margin: 0.5rem;
    box-shadow: 0px 1px 2px 0px #e3e3e3; 
/*     background:#f2f2f2; */
  }
  @media screen and (max-width: 600px) {
    td {
      word-break: break-word;
    }
  }
  td {
    border: 1px solid #bbb;
    padding:0.5em;
  }
</style>