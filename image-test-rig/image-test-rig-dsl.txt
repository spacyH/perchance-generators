$meta
  title       = Image Gen Test Rig
  description = Side-by-side image generation testing tool. Generate 4 variations with the same seed to verify which syntax features actually work — weight syntax, inline parameters, prompt editing, alternating, compositional, and BREAK.
  tags        = test, debug, image, t2i, stable-diffusion

image = {import:text-to-image-plugin}
